# fitness-timer-autopost
🔥 Автоматический постинг табата-тренировок в Telegram канал FITNESS TIMER через DeepSeek AI | Табата | HIIT | Фитнес

## 🛒 Автоматическое обновление цен продуктов

Проект автоматически обновляет цены продуктов из Пятёрочки каждую неделю.

### Как это работает:

1. **GitHub Actions** запускается каждое воскресенье в 06:00 МСК
2. Скрипт `scripts/update_prices.py` получает актуальные цены через API
3. Обновлённые данные сохраняются в `recipes_prices.json`
4. Если цена изменилась более чем на 20% — отправляется уведомление в Telegram

### Ручной запуск обновления:

```bash
python scripts/update_prices.py
```

## 🛒 Автоматический парсинг цен из ProShoper

Система автоматически собирает актуальные цены из каталогов акций ProShoper.ru и обновляет стоимость ингредиентов в рецептах.

### Как это работает:

1. **Еженедельный парсинг**: Каждую среду в 10:00 МСК запускается workflow
2. **Сбор данных**: Парсятся акционные цены из Пятёрочки (ProShoper.ru)
3. **Маппинг**: Ингредиенты автоматически сопоставляются с товарами
4. **Обновление**: Цены обновляются в `recipes_prices.json`
5. **Уведомление**: Статус отправляется в Telegram

### Файлы:

- `scripts/scrape_proshoper.py` - парсер ProShoper.ru
- `scripts/match_ingredients.py` - сопоставление ингредиентов
- `grocery_prices.json` - база акционных цен
- `.github/workflows/scrape-prices.yml` - автоматизация

### Запуск вручную:

```bash
pip install -r requirements-scraper.txt
python scripts/scrape_proshoper.py
python scripts/match_ingredients.py
```
