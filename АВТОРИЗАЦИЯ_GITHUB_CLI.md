# 🔐 АВТОРИЗАЦИЯ GITHUB CLI

## ⚠️ ВАЖНО

Авторизация GitHub CLI требует **интерактивного взаимодействия** (открытие браузера), поэтому Cursor не может выполнить её автоматически.

---

## 🚀 СПОСОБ 1: АВТОРИЗАЦИЯ ВРУЧНУЮ (РЕКОМЕНДУЕТСЯ)

### Выполните в терминале:

```bash
gh auth login
```

**Процесс:**
1. Выберите `GitHub.com`
2. Выберите `HTTPS` или `SSH` (рекомендуется `HTTPS`)
3. Выберите способ авторизации:
   - `Login with a web browser` (рекомендуется)
   - Или `Paste an authentication token`
4. Следуйте инструкциям на экране

**После авторизации:**
```bash
gh auth status
```

Должно показать: `✓ Logged in to github.com as [ваш_username]`

---

## 🔧 СПОСОБ 2: ЧЕРЕЗ ТОКЕН (БЕЗ БРАУЗЕРА)

### Если не хотите использовать браузер:

1. **Создайте Personal Access Token:**
   - Откройте: https://github.com/settings/tokens
   - Нажмите "Generate new token" → "Generate new token (classic)"
   - Название: `GitHub CLI`
   - Выберите scope: `repo`, `workflow`
   - Нажмите "Generate token"
   - **Скопируйте токен** (он показывается только один раз!)

2. **Авторизуйтесь с токеном:**
```bash
gh auth login --with-token
```

3. **Вставьте токен** и нажмите Enter

---

## ✅ ПРОВЕРКА АВТОРИЗАЦИИ

После авторизации проверьте:

```bash
gh auth status
```

Должно показать:
```
✓ Logged in to github.com as [ваш_username]
```

---

## 🚀 ПОСЛЕ АВТОРИЗАЦИИ

После успешной авторизации я смогу:

1. **Запускать workflow:**
```bash
gh workflow run "auto-reply.yml"
```

2. **Проверять статус:**
```bash
gh run list --workflow="auto-reply.yml" --limit 5
```

3. **Просматривать логи:**
```bash
gh run watch
```

---

## 📝 АЛЬТЕРНАТИВА: БЕЗ CLI

Если не хотите авторизовывать CLI, можно:

1. **Запускать workflow через браузер:**
   - Откройте: https://github.com/VR-Lounge/fitness-timer-autopost/actions/workflows/auto-reply.yml
   - Нажмите "Run workflow"

2. **Проверять логи через браузер:**
   - Откройте запуск
   - Просмотрите логи в интерфейсе

---

## 🎯 РЕКОМЕНДАЦИЯ

**Для разового запуска:** Используйте браузер (проще)

**Для автоматизации:** Авторизуйте CLI (удобнее для частого использования)

---

**После авторизации CLI сообщите мне, и я смогу запустить workflow автоматически!** 🚀

