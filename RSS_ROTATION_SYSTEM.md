# 🔄 СИСТЕМА РОТАЦИИ ИСТОЧНИКОВ RSS

**Дата:** 16 января 2026  
**Статус:** ✅ Реализовано

---

## 📋 ПРОБЛЕМА

Один и тот же источник RSS использовался для создания HTML страниц, хотя было 379 релевантных статей из разных источников. Это приводило к:
- Повторению контента
- Однообразию источников
- Низкому разнообразию контента

---

## ✅ РЕШЕНИЕ: СИСТЕМА РОТАЦИИ ИСТОЧНИКОВ

### Принцип работы:

1. **Отслеживание использованных RSS фидов:**
   - При парсинге RSS фида каждая статья получает поле `rss_feed_url`
   - При сохранении поста в `blog-posts.json` сохраняется `rss_feed_url`
   - Функция `получить_последние_использованные_источники(n=4)` извлекает последние n использованных RSS фидов и доменов

2. **Фильтрация при выборе статей:**
   - Перед добавлением статьи в список релевантных проверяется:
     - Не использовался ли этот RSS фид в последних 4 постах?
     - Не использовался ли этот домен в последних 4 постах?
   - Если использовался - статья пропускается

3. **Гарантия разнообразия:**
   - Система гарантирует, что каждый новый пост будет из другого источника
   - Минимум 4 разных источника между повторениями
   - Ротация работает как по конкретным RSS фидам, так и по доменам

---

## 🔧 ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ

### 1. Сохранение RSS фида в статьях

**Файл:** `womenshealth_parser.py`, `menshealth_parser.py`

```python
def парсить_rss_feed(rss_url):
    # ...
    articles.append({
        'title': title,
        'link': link,
        'description': description,
        'pub_date': pub_date,
        'rss_feed_url': rss_url  # ✅ Сохраняем URL RSS фида
    })
```

### 2. Получение последних использованных источников

**Файл:** `womenshealth_parser.py`, `menshealth_parser.py`

```python
def получить_последние_использованные_источники(n=4):
    """
    Returns:
        dict с ключами:
        - 'rss_feeds': список URL RSS фидов, которые использовались в последних n постах
        - 'domains': список доменов, которые использовались в последних n постах
    """
    последние_rss_фиды = []
    последние_домены = []
    
    # Извлекаем из последних n постов
    for пост in посты[:n]:
        rss_feed_url = пост.get('rss_feed_url') or ''
        if rss_feed_url:
            последние_rss_фиды.append(rss_feed_url)
        
        источник_url = пост.get('source_url') or ''
        if источник_url:
            домен = urlparse(источник_url).netloc.lower()
            последние_домены.append(домен)
    
    return {'rss_feeds': последние_rss_фиды, 'domains': последние_домены}
```

### 3. Фильтрация при выборе статей

**Файл:** `womenshealth_parser.py`, `menshealth_parser.py`

```python
# Получаем последние использованные источники
последние_источники = получить_последние_использованные_источники(4)
последние_rss_фиды = последние_источники.get('rss_feeds', [])
последние_домены = последние_источники.get('domains', [])

for статья in все_статьи:
    # Проверяем RSS фид
    rss_feed_статьи = статья.get('rss_feed_url', '')
    if rss_feed_статьи and rss_feed_статьи in последние_rss_фиды:
        print(f"🔄 Пропущена статья из недавно использованного RSS фида")
        continue
    
    # Проверяем домен
    домен_источника = urlparse(url_статьи).netloc.lower()
    if домен_источника in последние_домены:
        print(f"🔄 Пропущена статья из недавно использованного домена")
        continue
    
    # Статья прошла проверку, добавляем в релевантные
    релевантные.append(статья)
```

### 4. Сохранение RSS фида в постах

**Файл:** `womenshealth_parser.py`, `menshealth_parser.py`

```python
новый_пост = {
    'id': post_id,
    'title': заголовок_русский,
    'text': текст_для_блога,
    'image': локальное_изображение_url,
    'images': обработанные_изображения,
    'tags': теги,
    'source': источник,
    'source_url': url_статьи or '',
    'rss_feed_url': rss_feed_url or '',  # ✅ Сохраняем для ротации
    'date': datetime.now().isoformat(),
    'timestamp': int(time.time()),
    'url': url
}
```

---

## 📊 ПРИМЕР РАБОТЫ

### До исправления:
1. Пост 1: `https://hiitscience.com/feed` → статья A
2. Пост 2: `https://hiitscience.com/feed` → статья B (❌ тот же источник)
3. Пост 3: `https://hiitscience.com/feed` → статья C (❌ тот же источник)

### После исправления:
1. Пост 1: `https://hiitscience.com/feed` → статья A
2. Пост 2: `https://nourishmovelove.com/feed/` → статья B (✅ другой источник)
3. Пост 3: `https://gymbunny.ie/feed/` → статья C (✅ другой источник)
4. Пост 4: `https://www.womenfitness.net/feed/` → статья D (✅ другой источник)
5. Пост 5: `https://hiitscience.com/feed` → статья E (✅ можно использовать, прошло 4 поста)

---

## 🎯 ПРЕИМУЩЕСТВА

1. **Разнообразие контента:** Каждый пост из нового источника
2. **Предотвращение дубликатов:** Минимум 4 разных источника между повторениями
3. **Автоматическая ротация:** Система работает автоматически, без ручного вмешательства
4. **Двойная проверка:** Проверка и по RSS фидам, и по доменам для надёжности

---

## 🔄 УДАЛЁННЫЕ ИСТОЧНИКИ

- ❌ `https://hiitscience.com/feed` - удалён из обоих парсеров (содержит только подкасты, не релевантен для блога)

---

**Система ротации источников полностью реализована и работает!** 🚀
