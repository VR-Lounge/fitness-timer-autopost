# 🧪 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ СИСТЕМЫ АВТОПУБЛИКАЦИИ

## ✅ ЧТО УЖЕ ПРОВЕРЕНО

- ✅ Сайт открывается: https://www.tabatatimer.ru/blog.html
- ✅ Блог загружен на Яндекс Cloud
- ✅ Структура файлов корректна
- ✅ Workflow настроен правильно
- ✅ Все секреты настроены в GitHub

## 🚀 ЗАПУСК ТЕСТОВОГО ПАРСЕРА

### Вариант 1: Через GitHub Actions (РЕКОМЕНДУЕТСЯ)

1. **Перейдите на страницу workflow:**
   ```
   https://github.com/VR-Lounge/fitness-timer-autopost/actions/workflows/menshealth-parser.yml
   ```

2. **Нажмите кнопку "Run workflow"** (справа вверху, рядом с "Actions")

3. **Выберите ветку:** `main`

4. **Нажмите "Run workflow"**

5. **Дождитесь завершения** (обычно 2-5 минут)

6. **Проверьте результаты:**
   - ✅ Telegram канал - должен появиться новый пост
   - ✅ blog-posts.json - должен добавиться новый пост
   - ✅ HTML страница - должна сгенерироваться новая статья
   - ✅ Сайт - новый пост должен появиться в блоге

### Вариант 2: Локальный запуск

```bash
cd fitness-timer-autopost

# Установите переменные окружения
export TELEGRAM_BOT_TOKEN='ваш_токен'
export TELEGRAM_CHAT_ID='ваш_chat_id'
export DEEPSEEK_API_KEY='ваш_api_ключ'

# Запустите парсер
python3 menshealth_parser.py
```

## 📊 ПРОВЕРКА РЕЗУЛЬТАТОВ

### 1. Проверка Telegram канала

- Откройте ваш Telegram канал
- Должен появиться новый пост с фото и текстом
- Текст должен быть рерайтингом статьи из RSS-ленты

### 2. Проверка blog-posts.json

```bash
cd public_html
python3 -c "
import json
from pathlib import Path

blog_file = Path('blog-posts.json')
with open(blog_file, 'r', encoding='utf-8') as f:
    data = json.load(f)
    posts = data.get('posts', [])
    print(f'Всего постов: {len(posts)}')
    if posts:
        print(f'Последний пост: {posts[0].get(\"title\", \"N/A\")}')
        print(f'Дата: {posts[0].get(\"date\", \"N/A\")}')
"
```

### 3. Проверка HTML страниц

```bash
cd public_html/blog
ls -lh *.html | tail -1
```

Должна появиться новая HTML страница с уникальным slug.

### 4. Проверка сайта

- Откройте: https://www.tabatatimer.ru/blog.html
- Новый пост должен появиться в списке
- При клике должен открыться модальный окно с полным текстом

### 5. Проверка Яндекс Cloud

```bash
cd fitness-timer-autopost
./upload_blog_to_yandex.sh
```

Или проверьте в консоли:
https://console.yandex.cloud/folders/b1gmsesnb2h7cs5c7qov/storage/buckets/www.tabatatimer.ru

## 🔍 ПРОВЕРКА ЛОГОВ WORKFLOW

1. Перейдите: https://github.com/VR-Lounge/fitness-timer-autopost/actions
2. Выберите последний запуск workflow
3. Откройте детали выполнения
4. Проверьте логи каждого шага:
   - ✅ Клонирование репозитория
   - ✅ Установка зависимостей
   - ✅ Запуск парсера Men's Health
   - ✅ Генерация HTML страниц для блога
   - ✅ Загрузка файлов блога на Яндекс Cloud

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### Проблема: Workflow не запускается

**Решение:**
- Проверьте, что все секреты настроены в GitHub Secrets
- Убедитесь, что workflow файл находится в `.github/workflows/`
- Проверьте синтаксис YAML файла

### Проблема: Парсер не находит статьи

**Решение:**
- Проверьте, что RSS-ленты доступны
- Проверьте ключевые слова для фильтрации
- Проверьте логи workflow

### Проблема: Пост не публикуется в Telegram

**Решение:**
- Проверьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
- Убедитесь, что бот добавлен в канал как администратор
- Проверьте логи workflow

### Проблема: HTML страницы не генерируются

**Решение:**
- Проверьте, что `generate_blog_post_page.py` выполняется
- Проверьте права доступа к папке `public_html/blog/`
- Проверьте логи workflow

### Проблема: Файлы не загружаются на Яндекс Cloud

**Решение:**
- Проверьте секреты `YANDEX_ACCESS_KEY_ID` и `YANDEX_SECRET_ACCESS_KEY`
- Убедитесь, что у сервисного аккаунта есть права на запись
- Проверьте логи workflow

## ✅ ЧЕКЛИСТ ТЕСТИРОВАНИЯ

- [ ] Workflow запущен успешно
- [ ] Новый пост появился в Telegram канале
- [ ] Новый пост добавлен в blog-posts.json
- [ ] Новая HTML страница сгенерирована
- [ ] Новый пост появился на сайте
- [ ] Файлы загружены на Яндекс Cloud
- [ ] Все логи workflow без ошибок

## 📞 ПОДДЕРЖКА

Если возникли проблемы:
1. Проверьте логи workflow в GitHub Actions
2. Проверьте файл `AUTOPOST_SETUP.md` для подробной документации
3. Запустите `test_autopost_system.py` для диагностики

---

**Готово к тестированию!** 🚀
