# 🚀 БЫСТРЫЙ СТАРТ: АВТОНОМНАЯ РАБОТА CURSOR

## ⚡ За 5 минут

### 1. Установите зависимости:

```bash
cd fitness-timer-autopost
pip install -r requirements.txt
```

### 2. Запустите полный цикл:

```bash
python3 autonomous_main.py
```

### 3. Проверьте результаты:

```bash
# Отчеты тестирования
cat test_report.json

# Отчеты мониторинга
ls -lt monitoring_report_*.md | head -1 | xargs cat

# Итоговый отчет
ls -lt autonomous_summary_*.md | head -1 | xargs cat
```

## 🎯 Что делает система

1. ✅ **Тестирует код** - проверяет синтаксис, импорты, ищет баги
2. ✅ **Мониторит систему** - проверяет статус парсера, исправляет ошибки
3. ✅ **Генерирует инструкции** - создает задачи для Perplexity/Comet Browser
4. ✅ **Создает отчеты** - подробные отчеты о всех проверках

## 🔄 Автоматический режим

### GitHub Actions:

Система автоматически запускается каждые 6 часов через workflow `autonomous-monitoring.yml`.

### Локально:

Добавьте в cron:

```bash
# Каждые 2 часа
0 */2 * * * cd /path/to/fitness-timer-autopost && python3 autonomous_main.py
```

## 📋 Интеграция с Perplexity

1. **Cursor генерирует инструкции:**
   ```bash
   python3 browser_integration.py
   ```

2. **Perplexity выполняет:**
   - Читает `browser_test_*.md`
   - Проверяет сайты в Comet Browser
   - Сохраняет результаты в `browser_results_*.json`

3. **Cursor анализирует:**
   - Читает результаты
   - Применяет исправления
   - Генерирует новые инструкции

## 🎉 Готово!

Теперь Cursor работает автономно на уровне Senior программиста!

---

**Подробная документация:** `CURSOR_AUTONOMOUS_SETUP.md`
