# 🔍 Отладка системы автоматических ответов

## Проблема
Система не отвечает на комментарии в Telegram канале @fitnesstimer.

## Что проверить

### 1. Запускается ли workflow?

**Проверка через GitHub:**
1. Откройте https://github.com/VR-Lounge/fitness-timer-autopost/actions
2. Найдите workflow "🔄 Автоматический ответ на комментарии"
3. Проверьте, есть ли недавние запуски (должны быть каждые 30 минут)

**Если workflow не запускается:**
- Для приватных репозиториев на бесплатном плане cron может не работать
- **Решение:** Запустите workflow вручную через `workflow_dispatch`

### 2. Запуск workflow вручную

1. Откройте https://github.com/VR-Lounge/fitness-timer-autopost/actions/workflows/auto-reply.yml
2. Нажмите "Run workflow" → "Run workflow"
3. Проверьте логи выполнения

### 3. Проверка логов

После запуска workflow проверьте логи:
1. Откройте последний запуск workflow
2. Найдите шаг "Обработка комментариев и отправка ответов"
3. Проверьте вывод скрипта:
   - `🔍 Анализ сообщения:` - показывает, какие сообщения получены
   - `✅ Принят комментарий:` - показывает, какие комментарии обработаны
   - `⏭️ Пропускаем:` - показывает, почему комментарии пропускаются

### 4. Возможные проблемы

#### Проблема: Комментарий от бота
**Симптом:** Комментарий от "FITNESS TIMER" не обрабатывается
**Причина:** Система пропускает сообщения от ботов
**Решение:** Обновлён код - теперь комментарии к постам от ботов обрабатываются

#### Проблема: Комментарий не распознаётся
**Симптом:** Комментарий есть, но не обрабатывается
**Причина:** 
- Не является комментарием к посту (нет `reply_to_message`)
- Не из группы обсуждений
- Слишком короткий или длинный

**Решение:** Проверьте логи - там будет указана причина пропуска

#### Проблема: Workflow не запускается автоматически
**Симптом:** Нет запусков workflow по расписанию
**Причина:** Для приватных репозиториев cron может не работать
**Решение:** 
- Запускайте workflow вручную
- Или переведите репозиторий в публичный (если возможно)

### 5. Тестирование локально

Для тестирования можно запустить скрипт локально:

```bash
cd "/Users/LOBANOFF-PRO/Documents/TABATATIMER.RU/С MediaPipe/fitness-timer-autopost"

# Установите переменные окружения
export TELEGRAM_BOT_TOKEN="ваш_токен"
export TELEGRAM_CHAT_ID="ваш_chat_id"
export DEEPSEEK_API_KEY="ваш_ключ"

# Запустите скрипт
python auto_reply.py
```

### 6. Проверка настроек

Убедитесь, что:
1. ✅ Бот добавлен в группу обсуждений как администратор
2. ✅ В группе обсуждений включены комментарии к постам
3. ✅ Secrets в GitHub настроены правильно:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
   - `DEEPSEEK_API_KEY`

### 7. Улучшенное логирование

После последнего обновления добавлено детальное логирование:
- Показывает все полученные сообщения
- Показывает причины пропуска сообщений
- Показывает, какие комментарии обрабатываются
- Показывает время запуска

**Используйте логи для диагностики!**

## Следующие шаги

1. Запустите workflow вручную
2. Проверьте логи выполнения
3. Найдите причину, почему комментарии не обрабатываются
4. Сообщите о результатах для дальнейшей отладки

