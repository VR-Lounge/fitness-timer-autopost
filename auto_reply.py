#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
    Автоматический ответ на комментарии в Telegram
    
    Анализирует комментарии под постами через DeepSeek AI
    и автоматически отвечает на частые вопросы.
    
    Автор: VR-Lounge
    Канал: @fitnesstimer
"""

import os
import requests
import json
import re
import random
from datetime import datetime
from pathlib import Path

# Импортируем модуль статистики (используем sys.path для избежания конфликта с встроенным statistics)
import sys
from pathlib import Path
_statistics_path = Path(__file__).parent / 'statistics.py'
if _statistics_path.exists():
    try:
        import importlib.util
        spec = importlib.util.spec_from_file_location("telegram_statistics", _statistics_path)
        telegram_statistics = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(telegram_statistics)
        добавить_комментарий = telegram_statistics.добавить_комментарий
        отправить_summary = telegram_statistics.отправить_summary
        STATISTICS_AVAILABLE = True
    except Exception as e:
        print(f"⚠️ Ошибка загрузки модуля статистики: {e}")
        STATISTICS_AVAILABLE = False
else:
    print("⚠️ Модуль statistics.py не найден, статистика отключена")
    STATISTICS_AVAILABLE = False

# ============= КОНФИГУРАЦИЯ =============

TELEGRAM_BOT_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN')
TELEGRAM_CHAT_ID = os.getenv('TELEGRAM_CHAT_ID')
DEEPSEEK_API_KEY = os.getenv('DEEPSEEK_API_KEY')

# Файл для хранения последнего обработанного update_id
STATE_FILE = Path('.auto_reply_state.json')

# Файл для хранения уже отвеченных message_id (защита от дублирования)
ANSWERED_MESSAGES_FILE = Path('.answered_messages.json')

# Файл для хранения отзывов о таймере/сайте
FEEDBACK_FILE = Path('feedback_timer.md')

# Минимальная длина вопроса для ответа
MIN_QUESTION_LENGTH = 10

# Максимальная длина вопроса (чтобы не обрабатывать спам)
MAX_QUESTION_LENGTH = 500

# Ключевые слова, которые указывают на вопрос
QUESTION_KEYWORDS = [
    'как', 'что', 'почему', 'где', 'когда', 'сколько', 'можно', 
    'нужно', 'помоги', 'подскажи', 'объясни', 'расскажи',
    'какой', 'какая', 'какое', 'какие', 'чем', 'зачем'
]

# Слова-триггеры для автоматического ответа
AUTO_REPLY_TRIGGERS = [
    'таймер', 'tabata', 'hiit', 'amrap', 'emom', 'тренировка',
    'упражнение', 'программа', 'рецепт', 'питание', 'диета',
    'как начать', 'с чего начать', 'новичок', 'начальный',
    'сколько раз', 'сколько подходов', 'как делать'
]

# Ключевые слова для определения отзывов/вопросов про таймер/сайт
# ЗНАЧИТЕЛЬНО РАСШИРЕННАЯ КОЛЛЕКЦИЯ: учитывает разговорную речь, опечатки, разные формулировки
ТАЙМЕР_КЛЮЧЕВЫЕ_СЛОВА = [
    # Прямые упоминания таймера/сайта (все варианты)
    'tabatatimer', 'tabatatimer.ru', 'tabatatimer.com', 'www.tabatatimer',
    'tabata timer', 'tabata-timer', 'tabata_timer', 'табата таймер', 'табата-таймер',
    'таймер', 'таймера', 'таймеру', 'таймером', 'таймере',
    'сайт', 'сайта', 'сайте', 'сайту', 'сайтом', 'сайтом',
    'сайт tabatatimer', 'сайт табата', 'сайт таймер',
    'веб-сайт', 'вебсайт', 'веб сайт', 'сайтчик', 'сайтик',
    'портал', 'платформа', 'сервис', 'приложение', 'апп', 'app',
    
    # Проблемы с работой (много вариантов)
    'не работает', 'не работает таймер', 'не работает сайт',
    'не загружается', 'не загружается сайт', 'не грузится', 'не грузит',
    'глючит', 'глючит таймер', 'глючит сайт', 'глюк', 'глюки', 'глюкает',
    'баг', 'баги', 'багает', 'багованный', 'багнутый',
    'ошибка', 'ошибки', 'ошибается', 'выдает ошибку', 'выдает ошибки',
    'проблема', 'проблемы', 'проблемный', 'проблемка',
    'не открывается', 'не открывается сайт', 'не открывается таймер',
    'не запускается', 'не запускается таймер', 'не стартует',
    'тормозит', 'тормозит таймер', 'тормозит сайт', 'тормоз', 'тормозной',
    'лагает', 'лагает таймер', 'лагает сайт', 'лаги', 'лагает',
    'виснет', 'виснет таймер', 'виснет сайт', 'зависает', 'зависание',
    'падает', 'падает сайт', 'падает таймер', 'упал', 'упало',
    'сломался', 'сломался таймер', 'сломался сайт', 'сломано', 'сломали',
    'не функционирует', 'не функционирует таймер', 'не функционирует сайт',
    'не отвечает', 'не отвечает сайт', 'не отвечает таймер',
    'не реагирует', 'не реагирует таймер', 'не реагирует сайт',
    'не нажимается', 'не нажимается кнопка', 'кнопка не работает',
    'не кликается', 'не кликается кнопка', 'не нажимается',
    'не переключается', 'не переключается режим', 'режим не переключается',
    'не включается', 'не включается таймер', 'не включается музыка',
    'не выключается', 'не выключается таймер', 'не останавливается',
    'не сохраняется', 'не сохраняются настройки', 'настройки не сохраняются',
    'не загружается музыка', 'музыка не играет', 'звук не работает',
    'не слышно', 'не слышно звук', 'звук не работает', 'нет звука',
    'тихо', 'очень тихо', 'звук тихий', 'громкость не работает',
    
    # Вопросы об использовании таймера (не о тренировках!)
    'как пользоваться таймером', 'как использовать таймер', 'как включить таймер',
    'как пользоваться сайтом', 'как использовать сайт', 'как работать с таймером',
    'как запустить таймер', 'как начать таймер', 'как стартовать таймер',
    'как остановить таймер', 'как выключить таймер', 'как закончить таймер',
    'как настроить таймер', 'как настроить', 'как настраивать таймер',
    'как выбрать режим', 'как переключить режим', 'как сменить режим',
    'как выбрать музыку', 'как включить музыку', 'как выключить музыку',
    'как выбрать упражнение', 'как добавить упражнение', 'как убрать упражнение',
    'не понимаю таймер', 'не понимаю как пользоваться', 'не понимаю как работает',
    'не получается таймер', 'не получается запустить', 'не получается включить',
    'не знаю как', 'не знаю как пользоваться', 'не знаю как использовать',
    'как это работает', 'как работает таймер', 'как работает сайт',
    'что делать', 'что нужно делать', 'что нажать', 'куда нажать',
    'где кнопка', 'где найти', 'где находится', 'где искать',
    'не могу найти', 'не могу найти кнопку', 'не могу найти настройки',
    'не вижу', 'не вижу кнопку', 'не вижу настройки', 'не вижу меню',
    'не понятно', 'не понятно как', 'не понятно что делать',
    'не разобрался', 'не разобрался как', 'не могу разобраться',
    'помогите разобраться', 'помоги разобраться', 'помогите понять',
    'объясните как', 'объясни как', 'расскажите как', 'расскажи как',
    
    # Предложения и улучшения (много вариантов)
    'предложение', 'предложить', 'предлагаю', 'хочу предложить',
    'улучшить', 'улучшить таймер', 'улучшить сайт', 'улучшения',
    'добавить функцию', 'добавить функцию в таймер', 'добавить функцию на сайт',
    'добавить', 'добавить в таймер', 'добавить на сайт', 'добавьте',
    'хочу чтобы', 'хочу чтобы было', 'хотелось бы', 'хотелось бы чтобы',
    'было бы хорошо', 'было бы здорово', 'было бы классно', 'было бы круто',
    'можно добавить', 'можно было бы добавить', 'можно было бы сделать',
    'не хватает', 'не хватает функции', 'не хватает опции', 'не хватает кнопки',
    'хотелось бы добавить', 'хотелось бы видеть', 'хотелось бы иметь',
    'хочу видеть', 'хочу иметь', 'хочу чтобы добавили',
    'можно сделать', 'можно было бы сделать', 'можно улучшить',
    'стоит добавить', 'стоит сделать', 'стоит улучшить',
    'было бы полезно', 'было бы удобно', 'было бы приятно',
    'идея', 'идея для улучшения', 'идея для таймера', 'идея для сайта',
    'пожелание', 'пожелания', 'хотелось бы пожелать',
    'рекомендация', 'рекомендации', 'рекомендую добавить',
    'совет', 'советы', 'советую добавить', 'советую сделать',
    
    # Критика интерфейса/функционала (много вариантов)
    'неудобно', 'неудобный', 'неудобная', 'неудобное', 'неудобные',
    'неудобный интерфейс', 'неудобный сайт', 'неудобный таймер',
    'неудобно пользоваться', 'неудобно использовать', 'неудобно работать',
    'неудобная навигация', 'неудобное меню', 'неудобные кнопки',
    'сложно', 'сложный', 'сложная', 'сложное', 'сложные',
    'сложно пользоваться', 'сложно использовать', 'сложно разобраться',
    'сложный интерфейс', 'сложный сайт', 'сложный таймер',
    'непонятно', 'непонятный', 'непонятная', 'непонятное', 'непонятные',
    'непонятный интерфейс', 'непонятный сайт', 'непонятный таймер',
    'непонятно как пользоваться', 'непонятно как использовать',
    'непонятно что делать', 'непонятно куда нажимать',
    'плохо работает', 'плохо работает таймер', 'плохо работает сайт',
    'плохой интерфейс', 'плохой сайт', 'плохой таймер',
    'плохо сделано', 'плохо реализовано', 'плохо продумано',
    'не нравится', 'не нравится таймер', 'не нравится сайт',
    'не нравится интерфейс', 'не нравится дизайн', 'не нравится как работает',
    'не устраивает', 'не устраивает таймер', 'не устраивает сайт',
    'не устраивает интерфейс', 'не устраивает функционал',
    'неудовлетворительно', 'неудовлетворительный', 'неудовлетворительная работа',
    'разочарован', 'разочарована', 'разочарован таймером', 'разочарован сайтом',
    'не то что ожидал', 'не то что ожидала', 'не оправдал ожиданий',
    'можно лучше', 'можно было бы лучше', 'можно сделать лучше',
    'есть недостатки', 'есть минусы', 'есть проблемы',
    'не идеально', 'не идеальный', 'далеко от идеала',
    'хромает', 'хромает функционал', 'хромает интерфейс',
    'сыро', 'сырой', 'сырая версия', 'недоделанный',
    'нужно доработать', 'нужно доработать таймер', 'нужно доработать сайт',
    'требует доработки', 'требует улучшения', 'требует исправления',
    
    # Прямые отзывы (много вариантов)
    'отзыв', 'отзывы', 'отзыв о таймере', 'отзыв о сайте',
    'замечание', 'замечания', 'замечание к таймеру', 'замечание к сайту',
    'жалоба', 'жалобы', 'жалоба на таймер', 'жалоба на сайт',
    'критика', 'критиковать', 'критикую', 'критический отзыв',
    'фидбек', 'feedback', 'обратная связь', 'обратная связь о таймере',
    'мнение', 'мое мнение', 'мое мнение о таймере', 'мое мнение о сайте',
    'впечатление', 'впечатления', 'впечатление от таймера', 'впечатление от сайта',
    'оценка', 'оценить', 'оценка таймера', 'оценка сайта',
    'рецензия', 'рецензия на таймер', 'рецензия на сайт',
    'комментарий', 'комментарии', 'комментарий к таймеру', 'комментарий к сайту',
    'пожелание', 'пожелания', 'пожелание к таймеру', 'пожелание к сайту',
    'предложение', 'предложения', 'предложение по таймеру', 'предложение по сайту',
    
    # Технические проблемы (много вариантов)
    'браузер', 'браузере', 'в браузере', 'браузер не работает',
    'мобильный', 'мобильная версия', 'мобильный сайт', 'мобильный таймер',
    'на мобильном', 'на телефоне', 'на смартфоне', 'на айфоне', 'на андроиде',
    'телефон', 'на телефоне', 'с телефона', 'с телефона не работает',
    'планшет', 'на планшете', 'с планшета', 'с планшета не работает',
    'версия', 'версия сайта', 'версия таймера', 'какая версия',
    'обновление', 'обновить', 'обновить сайт', 'обновить таймер',
    'обновите', 'обновите сайт', 'обновите таймер', 'когда обновление',
    'старая версия', 'новая версия', 'последняя версия',
    'интернет', 'интернет не работает', 'нет интернета', 'проблемы с интернетом',
    'соединение', 'проблемы с соединением', 'плохое соединение',
    'загрузка', 'медленная загрузка', 'долго загружается', 'медленно загружается',
    'скорость', 'медленно', 'медленный', 'медленная работа',
    'производительность', 'проблемы с производительностью', 'низкая производительность',
    'оптимизация', 'нужна оптимизация', 'оптимизировать сайт',
    'адаптивность', 'не адаптивный', 'не адаптирован', 'не адаптируется',
    'responsive', 'не responsive', 'не отображается правильно',
    'дизайн', 'дизайн сайта', 'дизайн таймера', 'не нравится дизайн',
    'интерфейс', 'интерфейс сайта', 'интерфейс таймера', 'не нравится интерфейс',
    'юзабилити', 'usability', 'проблемы с юзабилити',
    'доступность', 'проблемы с доступностью', 'недоступен',
    'совместимость', 'проблемы с совместимостью', 'не совместим',
    'безопасность', 'проблемы с безопасностью', 'небезопасно',
    
    # Дополнительные варианты (опечатки, разговорная речь)
    'таймр', 'таймр не работает', 'таймр глючит',  # опечатка
    'сайт не рабтает', 'сайт не рабтает',  # опечатка
    'не рабтает', 'не рабтает таймер',  # опечатка
    'глюк', 'глюки', 'глюкает', 'глючит',
    'багает', 'багованный', 'багнутый',
    'лаги', 'лагает', 'лагит',
    'тормоз', 'тормозит', 'тормозной',
    'виснет', 'зависает', 'зависание',
    'упал', 'упало', 'падает',
    'сломался', 'сломано', 'сломали',
    'не раб', 'не раб таймер',  # сокращение
    'не раб сайт',  # сокращение
    'не пашет', 'не пашет таймер', 'не пашет сайт',  # разговорное
    'не канает', 'не канает таймер', 'не канает сайт',  # разговорное
    'не варит', 'не варит таймер', 'не варит сайт',  # разговорное
    'не шмогла', 'не шмогла таймер',  # разговорное/шутка
    'косяк', 'косяки', 'косячит', 'косячный',  # разговорное
    'глючный', 'глючный таймер', 'глючный сайт',  # разговорное
    'бажный', 'бажный таймер', 'бажный сайт',  # разговорное
    'лажный', 'лажный таймер', 'лажный сайт',  # разговорное
]

# Ключевые слова, которые указывают на ВОПРОС О ТРЕНИРОВКАХ (не о таймере!)
# ЗНАЧИТЕЛЬНО РАСШИРЕННАЯ КОЛЛЕКЦИЯ: учитывает разговорную речь, опечатки, разные формулировки
ТРЕНИРОВКА_КЛЮЧЕВЫЕ_СЛОВА = [
    # Типы тренировок (все варианты)
    'табата', 'табату', 'табатой', 'табате', 'табаты',
    'hiit', 'хиит', 'хиитом', 'хииту', 'хиите',
    'amrap', 'амрап', 'амрапом', 'амрапу', 'амрапе',
    'emom', 'емом', 'емомом', 'емому', 'емоме',
    'тренировка', 'тренировки', 'тренировку', 'тренировкой', 'тренировке',
    'тренировок', 'тренировкам', 'тренировками',
    'тренинг', 'тренинги', 'тренингом', 'тренингу', 'тренинге',
    'занятие', 'занятия', 'занятием', 'занятию', 'занятии',
    'тренируюсь', 'тренируешься', 'тренируется', 'тренируемся', 'тренируетесь',
    'тренироваться', 'тренироваться табата', 'тренироваться hiit',
    'занимаюсь', 'занимаешься', 'занимается', 'занимаемся', 'занимаетесь',
    'заниматься', 'заниматься табата', 'заниматься hiit',
    
    # Упражнения (много вариантов)
    'упражнение', 'упражнения', 'упражнению', 'упражнением', 'упражнении',
    'упражнений', 'упражнениям', 'упражнениями',
    'упражняюсь', 'упражняешься', 'упражняется', 'упражняемся',
    'упражняться', 'делаю упражнения', 'делаешь упражнения',
    'делать упражнения', 'какие упражнения', 'какие упражнения делать',
    'какое упражнение', 'какие упражнения выбрать',
    'выполняю', 'выполняешь', 'выполняет', 'выполняем', 'выполняете',
    'выполнять', 'выполнять упражнения', 'выполнение упражнений',
    'делаю', 'делаешь', 'делает', 'делаем', 'делаете',
    'делать', 'делать упражнения', 'делать тренировку',
    'как делать', 'как делать упражнение', 'как делать упражнения',
    'как выполнять', 'как выполнять упражнение', 'как выполнять упражнения',
    'техника', 'техника выполнения', 'техника упражнения',
    'правильная техника', 'как правильно делать',
    'правильно ли', 'правильно ли делаю', 'правильно ли выполняю',
    
    # Программы тренировок (много вариантов)
    'программа тренировок', 'программа тренировки', 'программы тренировок',
    'программа', 'программы', 'программу', 'программой', 'программе',
    'программ', 'программам', 'программами',
    'программа для', 'программа для похудения', 'программа для набора массы',
    'программа для начинающих', 'программа для новичков',
    'программа для продвинутых', 'программа для опытных',
    'тренировочная программа', 'тренировочные программы',
    'план тренировок', 'план тренировки', 'планы тренировок',
    'план', 'планы', 'планом', 'плану', 'плане',
    'схема тренировок', 'схема тренировки', 'схемы тренировок',
    'схема', 'схемы', 'схемой', 'схему', 'схеме',
    'комплекс', 'комплексы', 'комплексом', 'комплексу', 'комплексе',
    'комплекс упражнений', 'комплекс тренировок',
    'расписание', 'расписания', 'расписанием', 'расписанию', 'расписании',
    'расписание тренировок', 'как составить расписание',
    
    # Вопросы о тренировках (много вариантов)
    'как тренироваться', 'как тренироваться табата', 'как тренироваться hiit',
    'как заниматься', 'как заниматься табата', 'как заниматься hiit',
    'как начать', 'как начать тренироваться', 'как начать заниматься',
    'как начать табата', 'как начать hiit', 'как начать amrap',
    'с чего начать', 'с чего начать тренировки', 'с чего начать заниматься',
    'с чего начать табата', 'с чего начать hiit',
    'как правильно', 'как правильно тренироваться', 'как правильно заниматься',
    'как правильно делать', 'как правильно выполнять',
    'сколько раз', 'сколько раз делать', 'сколько раз выполнять',
    'сколько раз в неделю', 'сколько раз тренироваться',
    'сколько подходов', 'сколько подходов делать', 'сколько подходов нужно',
    'сколько повторений', 'сколько повторений делать', 'сколько повторений нужно',
    'сколько времени', 'сколько времени тренироваться', 'сколько времени заниматься',
    'сколько минут', 'сколько минут тренироваться', 'сколько минут делать',
    'сколько секунд', 'сколько секунд делать', 'сколько секунд отдыхать',
    'как часто', 'как часто тренироваться', 'как часто заниматься',
    'как долго', 'как долго тренироваться', 'как долго заниматься',
    'как долго делать', 'как долго выполнять',
    'как делать упражнение', 'как делать упражнения',
    'как выполнять упражнение', 'как выполнять упражнения',
    'как делать табата', 'как делать hiit', 'как делать amrap', 'как делать emom',
    'как выполнять табата', 'как выполнять hiit', 'как выполнять amrap', 'как выполнять emom',
    'что такое', 'что такое табата', 'что такое hiit', 'что такое amrap', 'что такое emom',
    'что это', 'что это табата', 'что это hiit', 'что это amrap', 'что это emom',
    'что значит', 'что значит табата', 'что значит hiit',
    'объясните', 'объясните табата', 'объясните hiit', 'объясните amrap',
    'объясни', 'объясни табата', 'объясни hiit', 'объясни amrap',
    'расскажите', 'расскажите о табата', 'расскажите о hiit',
    'расскажи', 'расскажи о табата', 'расскажи о hiit',
    'помогите', 'помогите с тренировкой', 'помогите с упражнениями',
    'помоги', 'помоги с тренировкой', 'помоги с упражнениями',
    'подскажите', 'подскажите как', 'подскажите что делать',
    'подскажи', 'подскажи как', 'подскажи что делать',
    'совет', 'совет по тренировке', 'совет по упражнениям',
    'советы', 'советы по тренировкам', 'советы по упражнениям',
    'рекомендация', 'рекомендации', 'рекомендации по тренировкам',
    'рекомендую', 'рекомендую тренировку', 'рекомендую упражнения',
    
    # Питание (много вариантов)
    'рецепт', 'рецепты', 'рецепту', 'рецептом', 'рецепте',
    'рецептов', 'рецептам', 'рецептами',
    'рецепт для', 'рецепт для похудения', 'рецепт для набора массы',
    'рецепт на', 'рецепт на завтрак', 'рецепт на обед', 'рецепт на ужин',
    'питание', 'питания', 'питанию', 'питанием', 'питании',
    'питание для', 'питание для похудения', 'питание для набора массы',
    'правильное питание', 'здоровое питание', 'спортивное питание',
    'диета', 'диеты', 'диете', 'диетой', 'диету',
    'диет', 'диетам', 'диетами',
    'диета для', 'диета для похудения', 'диета для набора массы',
    'калории', 'калорий', 'калориям', 'калориями',
    'сколько калорий', 'сколько калорий нужно', 'сколько калорий потреблять',
    'белок', 'белка', 'белку', 'белком', 'белке',
    'белков', 'белкам', 'белками',
    'сколько белка', 'сколько белка нужно', 'сколько белка потреблять',
    'протеин', 'протеина', 'протеину', 'протеином', 'протеине',
    'углеводы', 'углеводов', 'углеводам', 'углеводами',
    'сколько углеводов', 'сколько углеводов нужно',
    'жиры', 'жиров', 'жирам', 'жирами',
    'сколько жиров', 'сколько жиров нужно',
    'еда', 'еды', 'еде', 'едой', 'еду',
    'что есть', 'что есть до тренировки', 'что есть после тренировки',
    'что кушать', 'что кушать до тренировки', 'что кушать после тренировки',
    'что пить', 'что пить во время тренировки', 'что пить после тренировки',
    'питаться', 'как питаться', 'как правильно питаться',
    'кушать', 'как кушать', 'как правильно кушать',
    'есть', 'как есть', 'как правильно есть',
    
    # Цели тренировок (много вариантов)
    'похудение', 'похудения', 'похудению', 'похудением', 'похудении',
    'похудеть', 'хочу похудеть', 'нужно похудеть', 'как похудеть',
    'сбросить вес', 'сбросить лишний вес', 'сбросить килограммы',
    'снизить вес', 'снизить лишний вес', 'снизить вес',
    'набор массы', 'набора массы', 'набору массы', 'набором массы',
    'набрать массу', 'хочу набрать массу', 'нужно набрать массу', 'как набрать массу',
    'набрать вес', 'набрать мышечную массу', 'набрать мышцы',
    'выносливость', 'выносливости', 'выносливости', 'выносливостью',
    'развить выносливость', 'улучшить выносливость', 'как развить выносливость',
    'сила', 'силы', 'силе', 'силой',
    'развить силу', 'улучшить силу', 'как развить силу', 'как увеличить силу',
    'гибкость', 'гибкости', 'гибкости', 'гибкостью',
    'развить гибкость', 'улучшить гибкость', 'как развить гибкость',
    'рельеф', 'рельефа', 'рельефу', 'рельефом',
    'рельеф тела', 'как сделать рельеф', 'как получить рельеф',
    'пресс', 'пресса', 'прессу', 'прессом',
    'кубики пресса', 'как накачать пресс', 'как сделать пресс',
    'мышцы', 'мышц', 'мышцам', 'мышцами',
    'накачать мышцы', 'как накачать мышцы', 'как нарастить мышцы',
    'форма', 'формы', 'форме', 'формой',
    'привести в форму', 'как привести в форму', 'быть в форме',
    'здоровье', 'здоровья', 'здоровью', 'здоровьем',
    'укрепить здоровье', 'улучшить здоровье', 'как укрепить здоровье',
    
    # Уровни подготовки (много вариантов)
    'начинающий', 'начинающая', 'начинающее', 'начинающие',
    'начинающему', 'начинающей', 'начинающему', 'начинающим',
    'новичок', 'новичка', 'новичку', 'новичком', 'новичке',
    'новички', 'новичков', 'новичкам', 'новичками',
    'для начинающих', 'для новичков', 'программа для начинающих',
    'средний', 'средняя', 'среднее', 'средние',
    'среднему', 'средней', 'среднему', 'средним',
    'средний уровень', 'для среднего уровня', 'программа для среднего уровня',
    'продвинутый', 'продвинутая', 'продвинутое', 'продвинутые',
    'продвинутому', 'продвинутой', 'продвинутому', 'продвинутым',
    'продвинутый уровень', 'для продвинутых', 'программа для продвинутых',
    'опытный', 'опытная', 'опытное', 'опытные',
    'опытному', 'опытной', 'опытному', 'опытным',
    'опытный спортсмен', 'для опытных', 'программа для опытных',
    'профессионал', 'профессионала', 'профессионалу', 'профессионалом',
    'профессионалы', 'профессионалов', 'профессионалам', 'профессионалами',
    
    # Дополнительные варианты (опечатки, разговорная речь)
    'табату', 'табатой', 'табате',  # опечатки/варианты
    'хиит', 'хиитом', 'хииту',  # опечатки/варианты
    'амрап', 'амрапом', 'амрапу',  # опечатки/варианты
    'емом', 'емомом', 'емому',  # опечатки/варианты
    'тренируюсь', 'тренируешься', 'тренируется',  # разговорное
    'занимаюсь', 'занимаешься', 'занимается',  # разговорное
    'делаю', 'делаешь', 'делает',  # разговорное
    'качаюсь', 'качаешься', 'качается',  # разговорное
    'качаться', 'качаться в зале', 'качаться дома',
    'качаю', 'качаешь', 'качает',  # разговорное
    'качать', 'качать мышцы', 'качать пресс',
    'пашу', 'пашешь', 'пашет',  # разговорное
    'пахать', 'пахать в зале', 'пахать на тренировке',
    'работаю', 'работаешь', 'работает',  # разговорное
    'работать', 'работать в зале', 'работать на тренировке',
    'шпагат', 'шпагата', 'шпагату',  # разговорное
    'делать шпагат', 'как сделать шпагат',
    'растяжка', 'растяжки', 'растяжку',  # разговорное
    'делать растяжку', 'как делать растяжку',
    'разминка', 'разминки', 'разминку',  # разговорное
    'делать разминку', 'как делать разминку',
    'заминка', 'заминки', 'заминку',  # разговорное
    'делать заминку', 'как делать заминку',
]

# Слова, которые указывают на спам (не отвечаем)
SPAM_KEYWORDS = [
    'купить', 'продать', 'заработок', 'реклама', 'ссылка',
    'bit.ly', 't.me/', 'http://', 'https://', 'реферал'
]

# Короткие мотивационные фразы (обезличенные, в тему)
МОТИВАЦИОННЫЕ_ФРАЗЫ = [
    "Боль — это слабость, покидающая тело 💪",
    "Успех — это сумма маленьких усилий каждый день 🔥",
    "Единственный плохой тренинг — тот, который не состоялся 💯",
    "Тело может всё. Нужно убедить разум 🧠",
    "Не жди идеального момента. Начни сейчас 🚀",
    "Боль временна, гордость — навсегда 💎",
    "Каждый эксперт когда-то был новичком ⚡",
    "Прогресс важнее совершенства 🌟",
    "Твоё будущее создаётся тем, что ты делаешь сегодня 🎯",
    "Сложное становится простым с практикой 💪",
    "Не останавливайся, когда устал. Останавливайся, когда сделал 🔥",
    "Мотивация приходит и уходит. Дисциплина остаётся 💎",
    "Маленькие шаги ведут к большим целям 🚶",
    "Сегодняшняя боль — завтрашняя сила 💪",
    "Тренировка — это инвестиция в себя 🏆",
    "Лучшее время начать было вчера. Второе лучшее — сейчас ⏰",
    "Твой лимит — это только то, что ты себе установил 🚀",
    "Каждая тренировка приближает к цели 🎯",
    "Усталость — признак того, что ты на правильном пути 💯",
    "Не сравнивай себя с другими. Сравнивай с собой вчерашним 📈",
    "Терпение и упорство побеждают талант 🏅",
    "Тренируйся не потому что ненавидишь своё тело, а потому что любишь его ❤️",
    "Капля камень точит — регулярность важнее интенсивности 💧",
    "Сомнения убивают больше мечтаний, чем неудачи 🎭",
    "Начни там, где ты есть. Используй то, что имеешь 🛠️"
]

# Ссылки на SportWiki для разных тем
SPORTWIKI_ССЫЛКИ = {
    'табата': 'https://sportwiki.to/Табата',
    'hiit': 'https://sportwiki.to/ВИИТ',
    'тренировка': 'https://sportwiki.to/Тренировка',
    'питание': 'https://sportwiki.to/Спортивное_питание',
    'рецепт': 'https://sportwiki.to/Спортивная_диета',
    'упражнение': 'https://sportwiki.to/Упражнения',
    'программа': 'https://sportwiki.to/Тренировочная_программа',
    'начинающий': 'https://sportwiki.to/Новичок',
    'сила': 'https://sportwiki.to/Сила',
    'выносливость': 'https://sportwiki.to/Выносливость'
}


def загрузить_состояние():
    """Загружает последний обработанный update_id"""
    if STATE_FILE.exists():
        try:
            with open(STATE_FILE, 'r', encoding='utf-8') as f:
                data = json.load(f)
                return data.get('last_update_id', 0)
        except Exception as e:
            print(f"⚠️ Ошибка загрузки состояния: {e}")
    return 0


def сохранить_состояние(update_id):
    """Сохраняет последний обработанный update_id"""
    try:
        with open(STATE_FILE, 'w', encoding='utf-8') as f:
            json.dump({'last_update_id': update_id}, f, ensure_ascii=False, indent=2)
    except Exception as e:
        print(f"⚠️ Ошибка сохранения состояния: {e}")


def загрузить_отвеченные_сообщения():
    """Загружает список уже отвеченных message_id"""
    if ANSWERED_MESSAGES_FILE.exists():
        try:
            with open(ANSWERED_MESSAGES_FILE, 'r', encoding='utf-8') as f:
                data = json.load(f)
                # Возвращаем set для быстрой проверки
                answered = set(data.get('answered_message_ids', []))
                # Очищаем старые записи (старше 7 дней) для экономии места
                # Но пока просто возвращаем все
                return answered
        except Exception as e:
            print(f"⚠️ Ошибка загрузки отвеченных сообщений: {e}")
    return set()


def сохранить_отвеченное_сообщение(message_id):
    """Сохраняет message_id как отвеченный"""
    try:
        # Загружаем существующие
        answered = загрузить_отвеченные_сообщения()
        # Добавляем новый
        answered.add(str(message_id))
        
        # Ограничиваем размер (храним последние 10000 сообщений)
        if len(answered) > 10000:
            # Преобразуем в список, отсортируем и возьмём последние 10000
            answered_list = sorted([int(mid) for mid in answered if mid.isdigit()])
            answered = set(str(mid) for mid in answered_list[-10000:])
        
        # Сохраняем
        with open(ANSWERED_MESSAGES_FILE, 'w', encoding='utf-8') as f:
            json.dump({'answered_message_ids': list(answered)}, f, ensure_ascii=False, indent=2)
        
        return True
    except Exception as e:
        print(f"⚠️ Ошибка сохранения отвеченного сообщения: {e}")
        return False


def уже_отвечено(message_id):
    """Проверяет, был ли уже отправлен ответ на это сообщение"""
    answered = загрузить_отвеченные_сообщения()
    return str(message_id) in answered


def получить_новые_комментарии(last_update_id):
    """
    Получает новые комментарии из Telegram канала.
    Использует getUpdates для получения сообщений из канала и группы обсуждений.
    
    ВАЖНО: Для работы с комментариями к постам:
    1. У канала должна быть включена группа обсуждений
    2. Бот должен быть администратором группы обсуждений
    3. Комментарии к постам приходят как сообщения в группу обсуждений
    """
    # ПРОВЕРКА ДОСТУПА: Проверяем, может ли бот видеть группу обсуждений
    try:
        # Получаем информацию о канале
        channel_info_url = f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/getChat"
        channel_info_params = {'chat_id': TELEGRAM_CHAT_ID}
        channel_info_response = requests.get(channel_info_url, params=channel_info_params, timeout=10)
        if channel_info_response.status_code == 200:
            channel_data = channel_info_response.json()
            if channel_data.get('ok'):
                channel_info = channel_data.get('result', {})
                linked_chat_id = channel_info.get('linked_chat_id')
                print(f"🔍 ПРОВЕРКА ДОСТУПА: Канал {TELEGRAM_CHAT_ID}")
                print(f"   - linked_chat_id (группа обсуждений): {linked_chat_id}")
                if linked_chat_id:
                    # Проверяем доступ к группе обсуждений
                    group_info_params = {'chat_id': linked_chat_id}
                    group_info_response = requests.get(channel_info_url, params=group_info_params, timeout=10)
                    if group_info_response.status_code == 200:
                        group_data = group_info_response.json()
                        if group_data.get('ok'):
                            group_info = group_data.get('result', {})
                            print(f"   - Группа обсуждений доступна: {group_info.get('title', 'unknown')}")
                            print(f"   - Тип группы: {group_info.get('type', 'unknown')}")
                            
                            # КРИТИЧЕСКАЯ ПРОВЕРКА: Проверяем права бота в группе
                            try:
                                bot_info_url = f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/getChatMember"
                                bot_info_params = {
                                    'chat_id': linked_chat_id,
                                    'user_id': TELEGRAM_BOT_TOKEN.split(':')[0]  # Получаем bot_id из токена
                                }
                                # Альтернативный способ: используем getMe для получения bot_id
                                get_me_url = f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/getMe"
                                get_me_response = requests.get(get_me_url, timeout=10)
                                if get_me_response.status_code == 200:
                                    me_data = get_me_response.json()
                                    if me_data.get('ok'):
                                        bot_id = me_data.get('result', {}).get('id')
                                        print(f"   - Bot ID: {bot_id}")
                                        bot_info_params['user_id'] = bot_id
                                        bot_member_response = requests.get(bot_info_url, params=bot_info_params, timeout=10)
                                        if bot_member_response.status_code == 200:
                                            bot_member_data = bot_member_response.json()
                                            if bot_member_data.get('ok'):
                                                bot_member = bot_member_data.get('result', {})
                                                bot_status = bot_member.get('status', 'unknown')
                                                print(f"   - Статус бота в группе: {bot_status}")
                                                if bot_status not in ['administrator', 'member']:
                                                    print(f"   ⚠️ ВНИМАНИЕ: Бот не является участником группы! Статус: {bot_status}")
                                                    print(f"   ⚠️ Бот должен быть добавлен в группу обсуждений как администратор или участник!")
                                            else:
                                                print(f"   ⚠️ Не удалось получить информацию о боте в группе: {bot_member_data.get('description')}")
                                        else:
                                            print(f"   ⚠️ Ошибка проверки прав бота: HTTP {bot_member_response.status_code}")
                            except Exception as e:
                                print(f"   ⚠️ Ошибка проверки прав бота: {e}")
                        else:
                            print(f"   ⚠️ Не удалось получить информацию о группе: {group_data.get('description')}")
                    else:
                        print(f"   ⚠️ Ошибка доступа к группе: HTTP {group_info_response.status_code}")
                else:
                    print(f"   ⚠️ У канала нет привязанной группы обсуждений!")
            else:
                print(f"   ⚠️ Не удалось получить информацию о канале: {channel_data.get('description')}")
        else:
            print(f"   ⚠️ Ошибка доступа к каналу: HTTP {channel_info_response.status_code}")
    except Exception as e:
        print(f"⚠️ Ошибка проверки доступа: {e}")
    
    url = f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/getUpdates"
    
    # КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Пробуем получить ВСЕ обновления без фильтрации allowed_updates
    # для поиска комментариев, которые могут приходить в другом формате
    print(f"🔍 Пробуем получить обновления БЕЗ фильтрации allowed_updates...")
    
    # Пробуем несколько вариантов offset для поиска комментария
    search_offsets = [
        max(0, last_update_id - 500),  # Последние 500 обновлений
        max(0, last_update_id - 1000),  # Последние 1000 обновлений
        0  # Все обновления (если нужно)
    ]
    
    found_comment = False
    for search_offset in search_offsets:
        if found_comment:
            break
        all_updates_params = {
            'offset': search_offset,
            'timeout': 5,
            'limit': 100
            # НЕ указываем allowed_updates - получаем ВСЕ типы обновлений
        }
        try:
            print(f"🔍 Поиск в диапазоне offset={search_offset}...")
            all_updates_response = requests.get(url, params=all_updates_params, timeout=10)
            if all_updates_response.status_code == 200:
                all_updates_data = all_updates_response.json()
                if all_updates_data.get('ok'):
                    all_updates = all_updates_data.get('result', [])
                    print(f"🔍 БЕЗ ФИЛЬТРАЦИИ (offset={search_offset}): Получено {len(all_updates)} обновлений")
                    if len(all_updates) > 0:
                        print(f"🔍 Анализируем все обновления на наличие комментариев...")
                        for upd in all_updates:
                            upd_id = upd.get('update_id', 0)
                            if 'message' in upd:
                                msg = upd.get('message', {})
                                chat_type = msg.get('chat', {}).get('type', 'unknown')
                                chat_id = msg.get('chat', {}).get('id', 'unknown')
                                from_user = msg.get('from', {})
                                username = from_user.get('username', 'unknown')
                                first_name = from_user.get('first_name', 'unknown')
                                text_preview = (msg.get('text') or msg.get('caption', ''))[:100]
                                reply_to = msg.get('reply_to_message')
                                has_reply = reply_to is not None
                                # Проверяем, может ли это быть комментарий
                                if chat_type in ['supergroup', 'group'] or has_reply:
                                    print(f"   ✅ update_id={upd_id}: chat_type={chat_type}, chat_id={chat_id}, from={first_name} (@{username}), reply_to={has_reply}")
                                    print(f"      text={text_preview}...")
                                    if 'творож' in text_preview.lower() or 'запекан' in text_preview.lower() or '187' in text_preview or '93' in text_preview:
                                        print(f"      🎯 НАЙДЕН КОММЕНТАРИЙ О ТВОРОЖНОЙ ЗАПЕКАНКЕ!")
                                        found_comment = True
                                        # НЕ прерываем цикл - продолжаем искать все комментарии
                    else:
                        print(f"   ⚠️ Нет обновлений в диапазоне offset={search_offset}")
                        break  # Если нет обновлений, не проверяем дальше
        except Exception as e:
            print(f"⚠️ Ошибка получения всех обновлений (offset={search_offset}): {e}")
    
    params = {
        'offset': last_update_id + 1,
        'timeout': 10,
        # ВАЖНО: НЕ включаем 'channel_post' - нам нужны только комментарии, а не посты канала!
        # Но включаем все типы сообщений для диагностики
        'allowed_updates': ['message']  # Только сообщения из группы обсуждений
    }
    
    # ДИАГНОСТИКА: Проверяем последние 100 обновлений для поиска комментариев
    try:
        print(f"🔍 ДИАГНОСТИКА: Проверяем последние 100 обновлений (offset={max(0, last_update_id - 100)})...")
        diagnostic_params = {
            'offset': max(0, last_update_id - 100),  # Проверяем последние 100 обновлений
            'timeout': 5,
            'limit': 100
        }
        diagnostic_response = requests.get(url, params=diagnostic_params, timeout=10)
        if diagnostic_response.status_code == 200:
            diagnostic_data = diagnostic_response.json()
            if diagnostic_data.get('ok'):
                diagnostic_updates = diagnostic_data.get('result', [])
                print(f"🔍 ДИАГНОСТИКА: Всего обновлений доступно (offset={diagnostic_params['offset']}): {len(diagnostic_updates)}")
                if len(diagnostic_updates) > 0:
                    print(f"🔍 ДИАГНОСТИКА: Анализируем первые 10 обновлений:")
                    for diag_update in diagnostic_updates[:10]:
                        diag_update_id = diag_update.get('update_id', 0)
                        has_message = 'message' in diag_update
                        has_channel_post = 'channel_post' in diag_update
                        print(f"   - update_id={diag_update_id}: message={has_message}, channel_post={has_channel_post}")
                        if has_message:
                            msg = diag_update.get('message', {})
                            chat_type = msg.get('chat', {}).get('type', 'unknown')
                            chat_id = msg.get('chat', {}).get('id', 'unknown')
                            from_user = msg.get('from', {})
                            username = from_user.get('username', 'unknown')
                            first_name = from_user.get('first_name', 'unknown')
                            is_bot = from_user.get('is_bot', False)
                            text_preview = (msg.get('text') or msg.get('caption', ''))[:50]
                            reply_to = msg.get('reply_to_message')
                            has_reply = reply_to is not None
                            print(f"     → chat_type={chat_type}, chat_id={chat_id}, from={first_name} (@{username}), is_bot={is_bot}, reply_to={has_reply}, text={text_preview}...")
                            if has_reply:
                                reply_chat_type = reply_to.get('chat', {}).get('type', 'unknown')
                                reply_chat_id = reply_to.get('chat', {}).get('id', 'unknown')
                                print(f"       → reply_to: chat_type={reply_chat_type}, chat_id={reply_chat_id}")
                else:
                    print(f"⚠️ ДИАГНОСТИКА: Нет обновлений в диапазоне offset={diagnostic_params['offset']} до {last_update_id + 1}")
                    print(f"⚠️ Это может означать:")
                    print(f"   1. Комментарий был отправлен до update_id={last_update_id + 1} и уже обработан")
                    print(f"   2. Бот не имеет доступа к группе обсуждений")
                    print(f"   3. Комментарии приходят в другом формате")
            else:
                print(f"❌ ДИАГНОСТИКА: Telegram API вернул ошибку: {diagnostic_data.get('description')}")
        else:
            print(f"❌ ДИАГНОСТИКА: Ошибка HTTP {diagnostic_response.status_code}")
    except Exception as e:
        print(f"⚠️ Ошибка диагностики: {e}")
        import traceback
        print(f"   Traceback: {traceback.format_exc()}")
    
    try:
        print(f"🔍 Запрос getUpdates: offset={last_update_id + 1}, timeout=10")
        response = requests.get(url, params=params, timeout=15)
        if response.status_code != 200:
            print(f"❌ Ошибка получения обновлений: {response.status_code}")
            return [], last_update_id
        
        data = response.json()
        if not data.get('ok'):
            print(f"❌ Telegram API вернул ошибку: {data.get('description')}")
            return [], last_update_id
        
        updates = data.get('result', [])
        print(f"📊 Получено обновлений от Telegram API: {len(updates)} шт.")
        if len(updates) > 0:
            print(f"🔍 Первые 3 update_id: {[u.get('update_id') for u in updates[:3]]}")
        комментарии = []
        max_update_id = last_update_id
        
        for update in updates:
            update_id = update.get('update_id', 0)
            max_update_id = max(max_update_id, update_id)
            
            print(f"🔍 Обработка update_id={update_id}")
            
            # ВАЖНО: Игнорируем посты из канала (channel_post) - это не комментарии!
            if update.get('channel_post'):
                print(f"⏭️ Пропускаем пост из канала (channel_post)")
                continue
            
            # Получаем сообщение (только из группы обсуждений или комментарии)
            message = update.get('message')
            if not message:
                print(f"⏭️ Пропускаем: нет поля 'message' в update")
                continue
            
            print(f"✅ Найдено сообщение в update_id={update_id}")
            
            # Сначала получаем информацию о чате и пользователе
            chat = message.get('chat', {})
            chat_id = str(chat.get('id', ''))
            chat_type = chat.get('type', '')
            
            from_user = message.get('from', {})
            is_bot = from_user.get('is_bot', False)
            username = from_user.get('username', '')
            first_name = from_user.get('first_name', '')
            
            # Логируем информацию о сообщении для отладки
            print(f"🔍 Анализ сообщения: from={first_name} (@{username}), is_bot={is_bot}, chat_type={chat_type}, chat_id={chat_id}")
            
            # КРИТИЧЕСКИ ВАЖНО: НЕ обрабатываем посты из самого канала!
            # Посты из канала имеют chat_type='channel' и chat_id == TELEGRAM_CHAT_ID
            # Мы обрабатываем ТОЛЬКО комментарии к постам!
            
            # Проверяем, что это НЕ пост из канала
            if chat_type == 'channel':
                # Если это канал - это точно пост, а не комментарий
                print(f"⏭️ Пропускаем: это пост из канала (chat_type=channel, chat_id={chat_id})")
                continue
            
            # Дополнительная проверка: если chat_id совпадает с ID канала - это пост
            if chat_id == str(TELEGRAM_CHAT_ID):
                print(f"⏭️ Пропускаем: сообщение из канала (chat_id совпадает с TELEGRAM_CHAT_ID)")
                continue
            
            # Комментарии к постам приходят из группы обсуждений (supergroup/group)
            # И имеют reply_to_message, указывающее на пост в канале
            is_comment_to_post = False
            reply_to = message.get('reply_to_message')
            reply_thread_id = None
            if reply_to:
                # Если это ответ на сообщение из канала - это комментарий к посту
                reply_chat = reply_to.get('chat', {})
                reply_chat_type = reply_chat.get('type', '')
                reply_chat_id = str(reply_chat.get('id', ''))
                
                # Комментарий к посту: reply_to указывает на канал
                if reply_chat_type == 'channel' and reply_chat_id == str(TELEGRAM_CHAT_ID):
                    is_comment_to_post = True
                    print(f"✅ Найден комментарий к посту (reply_to канал)")
                    # Получаем thread_id из исходного сообщения (поста)
                    reply_thread_id = reply_to.get('message_thread_id')
                    if reply_thread_id:
                        print(f"📌 Найден message_thread_id из исходного поста: {reply_thread_id}")
            
            # Также проверяем, что это сообщение из группы обсуждений (supergroup/group)
            is_from_discussion_group = chat_type in ['supergroup', 'group']
            
            # КРИТИЧЕСКИ ВАЖНО: Проверяем, что это НЕ пост из канала
            # Посты из канала могут приходить как message с chat_type='channel'
            # Или могут иметь признаки поста (например, sender_chat указывает на канал)
            sender_chat = message.get('sender_chat', {})
            if sender_chat:
                sender_chat_type = sender_chat.get('type', '')
                sender_chat_id = str(sender_chat.get('id', ''))
                # Если отправитель - канал, это пост, а не комментарий
                if sender_chat_type == 'channel' and sender_chat_id == str(TELEGRAM_CHAT_ID):
                    print(f"⏭️ Пропускаем: это пост из канала (sender_chat=channel)")
                    continue
            
            # Принимаем сообщение ТОЛЬКО если:
            # - Это комментарий к посту (reply_to указывает на канал), ИЛИ
            # - Это из группы обсуждений (supergroup/group) И НЕ из канала
            # НЕ принимаем посты из самого канала!
            if not (is_comment_to_post or (is_from_discussion_group and chat_id != str(TELEGRAM_CHAT_ID))):
                print(f"⏭️ Пропускаем: не комментарий к посту (chat_type={chat_type}, chat_id={chat_id}, is_comment={is_comment_to_post}, is_group={is_from_discussion_group})")
                continue
            
            # Теперь проверяем, является ли это ботом (ПОСЛЕ того как определили, что это комментарий)
            # Если это комментарий к посту от бота - можем обработать (иногда боты оставляют полезные комментарии)
            if is_bot:
                if is_comment_to_post:
                    # Это комментарий к посту от бота - можем обработать
                    print(f"⚠️ Комментарий от бота, но это комментарий к посту - обрабатываем")
                else:
                    # Это не комментарий к посту, а просто сообщение от бота - пропускаем
                    print(f"⏭️ Пропускаем: сообщение от бота (не комментарий к посту)")
                    continue
            
            # Получаем текст сообщения
            text = message.get('text') or message.get('caption', '')
            if not text or len(text.strip()) < MIN_QUESTION_LENGTH:
                print(f"⏭️ Пропускаем: текст слишком короткий ({len(text.strip()) if text else 0} символов, минимум {MIN_QUESTION_LENGTH})")
                continue
            
            # Проверяем, что это не слишком длинное сообщение (возможно спам)
            if len(text) > MAX_QUESTION_LENGTH:
                print(f"⏭️ Пропускаем: текст слишком длинный ({len(text)} символов, максимум {MAX_QUESTION_LENGTH})")
                continue
            
            print(f"✅ Принят комментарий: от {first_name} (@{username}), текст: {text[:50]}...")
            
            # ДЕТАЛЬНОЕ ЛОГИРОВАНИЕ для отладки структуры комментария
            print(f"🔍 ДЕТАЛЬНАЯ ДИАГНОСТИКА комментария:")
            print(f"   - message_id: {message.get('message_id')}")
            print(f"   - chat_id: {chat_id}")
            print(f"   - chat_type: {chat_type}")
            print(f"   - is_comment_to_post: {is_comment_to_post}")
            print(f"   - reply_to присутствует: {reply_to is not None}")
            if reply_to:
                print(f"   - reply_to.chat.type: {reply_to.get('chat', {}).get('type')}")
                print(f"   - reply_to.chat.id: {reply_to.get('chat', {}).get('id')}")
                print(f"   - reply_to.message_id: {reply_to.get('message_id')}")
                print(f"   - reply_to.message_thread_id: {reply_to.get('message_thread_id')}")
            print(f"   - message.message_thread_id: {message.get('message_thread_id')}")
            
            # Получаем message_thread_id (topic) для каналов с форумами
            message_thread_id = message.get('message_thread_id')
            if message_thread_id:
                print(f"📌 Найден message_thread_id (topic) в сообщении: {message_thread_id}")
            
            # Если это комментарий к посту, приоритет отдаём thread_id из reply_to_message
            # (это ID топика исходного поста в канале)
            if reply_to and is_comment_to_post and reply_thread_id:
                message_thread_id = reply_thread_id
                print(f"📌 Используем message_thread_id из reply_to (приоритет): {reply_thread_id}")
            
            # ВАЖНО: Если message_thread_id всё ещё None, но это комментарий к посту,
            # возможно, нужно получить его из исходного поста через API
            if not message_thread_id and is_comment_to_post and reply_to:
                reply_message_id = reply_to.get('message_id')
                if reply_message_id:
                    print(f"⚠️ message_thread_id не найден, но есть reply_to.message_id={reply_message_id}")
                    print(f"   Попробуем получить информацию о посте через API...")
                    # Получаем информацию о посте из канала
                    try:
                        get_url = f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/getChat"
                        get_params = {'chat_id': TELEGRAM_CHAT_ID}
                        get_response = requests.get(get_url, params=get_params, timeout=5)
                        if get_response.status_code == 200:
                            chat_info = get_response.json()
                            print(f"   Информация о канале: {chat_info}")
                    except Exception as e:
                        print(f"   Ошибка получения информации о канале: {e}")
            
            комментарии.append({
                'update_id': update_id,
                'message_id': message.get('message_id'),
                'text': text.strip(),
                'from_user': first_name or 'Пользователь',
                'username': username,
                'date': message.get('date', 0),
                'is_comment': is_comment_to_post or is_from_discussion_group,  # Флаг, что это комментарий
                'chat_id': chat_id,  # ВАЖНО: сохраняем chat_id группы обсуждений для отправки ответа
                'message_thread_id': message_thread_id  # ВАЖНО: для каналов с форумами (topics)
            })
        
        return комментарии, max_update_id
        
    except Exception as e:
        print(f"❌ Исключение при получении комментариев: {e}")
        return [], last_update_id


def это_вопрос(текст):
    """Проверяет, является ли текст вопросом"""
    текст_нижний = текст.lower()
    
    # Проверяем наличие вопросительного знака
    if '?' in текст:
        return True
    
    # Проверяем ключевые слова вопросов
    for keyword in QUESTION_KEYWORDS:
        if keyword in текст_нижний:
            return True
    
    return False


def это_спам(текст):
    """Проверяет, является ли текст спамом"""
    текст_нижний = текст.lower()
    
    for spam_word in SPAM_KEYWORDS:
        if spam_word in текст_нижний:
            return True
    
    return False


def нужно_отвечать(текст):
    """Определяет, нужно ли отвечать на комментарий"""
    if not это_вопрос(текст):
        return False
    
    if это_спам(текст):
        return False
    
    текст_нижний = текст.lower()
    
    # Проверяем наличие релевантных триггеров
    for trigger in AUTO_REPLY_TRIGGERS:
        if trigger in текст_нижний:
            return True
    
    # Если это вопрос и достаточно длинный - вероятно, стоит ответить
    if len(текст) >= 20:
        return True
    
    return False


def получить_релевантную_ссылку_sportwiki(вопрос):
    """Определяет релевантную ссылку на SportWiki по вопросу"""
    вопрос_нижний = вопрос.lower()
    for ключ, ссылка in SPORTWIKI_ССЫЛКИ.items():
        if ключ in вопрос_нижний:
            return ссылка
    return None


def получить_случайную_фразу():
    """Возвращает случайную короткую мотивационную фразу"""
    return random.choice(МОТИВАЦИОННЫЕ_ФРАЗЫ)


def получить_шутку():
    """Возвращает случайную фитнес-шутку"""
    шутки = [
        "Помни: мышцы растут не в зале, а когда ты думаешь о них дома 😄",
        "Отжимания — это когда ты поднимаешь себя от пола, а не от дивана 😂",
        "Бег — это когда ты убегаешь от проблем, но они бегут быстрее 🏃",
        "Планка — это когда минута кажется вечностью, но ты всё равно держишься 💪",
        "Приседания — это когда твои ноги говорят 'стоп', а ты говоришь 'ещё 5' 😅",
        "Кардио — это когда ты потеешь, но знаешь, что это красиво выглядит 🔥",
        "Растяжка — это когда ты понимаешь, что не такой гибкий, как думал 🤸",
        "Тренировка — это когда ты жалуешься, но всё равно идёшь завтра 😄",
        "Жим лёжа — это когда штанга кажется легче в голове, чем в руках 😂",
        "Йога — это когда ты пытаешься быть спокойным, но мышцы кричат 🧘"
    ]
    return random.choice(шутки)


def это_отзыв_про_таймер(текст):
    """
    УЛУЧШЕННАЯ ЛОГИКА: Проверяет, является ли текст отзывом/вопросом про таймер/сайт
    
    Логика:
    1. Если есть прямые упоминания таймера/сайта - это отзыв
    2. Если есть проблемы с работой + упоминание таймера - это отзыв
    3. Если есть предложения/критика + упоминание таймера - это отзыв
    4. Если есть только вопросы о тренировках БЕЗ упоминания таймера - это НЕ отзыв
    """
    текст_нижний = текст.lower()
    
    # Счётчики для определения типа сообщения
    упоминаний_таймера = 0
    упоминаний_проблем = 0
    упоминаний_тренировок = 0
    
    # Проверяем упоминания таймера/сайта
    таймер_слова = ['tabatatimer', 'tabatatimer.ru', 'таймер', 'сайт', 'сайта', 'сайте', 'сайту']
    for слово in таймер_слова:
        if слово in текст_нижний:
            упоминаний_таймера += 1
    
    # Проверяем проблемы с работой
    проблемы = ['не работает', 'не загружается', 'глючит', 'баг', 'ошибка', 'проблема',
                'не открывается', 'не запускается', 'тормозит', 'лагает', 'виснет',
                'неудобно', 'неудобный', 'сложно', 'непонятно']
    for проблема in проблемы:
        if проблема in текст_нижний:
            упоминаний_проблем += 1
    
    # Проверяем упоминания тренировок (без таймера)
    тренировка_слова = ['табата', 'hiit', 'amrap', 'emom', 'тренировка', 'упражнение']
    for слово in тренировка_слова:
        if слово in текст_нижний:
            упоминаний_тренировок += 1
    
    # ЛОГИКА ОПРЕДЕЛЕНИЯ:
    # 1. Если есть прямое упоминание таймера/сайта - это отзыв
    if упоминаний_таймера > 0:
        return True
    
    # 2. Если есть проблемы + контекст указывает на таймер (но нет прямого упоминания)
    # Проверяем через ключевые слова из ТАЙМЕР_КЛЮЧЕВЫЕ_СЛОВА
    if упоминаний_проблем > 0:
        for ключ in ТАЙМЕР_КЛЮЧЕВЫЕ_СЛОВА:
            if ключ in текст_нижний:
                return True
    
    # 3. Если есть только вопросы о тренировках БЕЗ упоминания таймера - это НЕ отзыв
    if упоминаний_тренировок > 0 and упоминаний_таймера == 0:
        return False
    
    # 4. Проверяем остальные ключевые слова (предложения, критика и т.д.)
    for ключ in ТАЙМЕР_КЛЮЧЕВЫЕ_СЛОВА:
        if ключ in текст_нижний:
            return True
    
    return False


def сохранить_отзыв_в_markdown(комментарий, ответ_пользователю):
    """Сохраняет отзыв о таймере в markdown файл"""
    try:
        # Создаём заголовок, если файл не существует
        if not FEEDBACK_FILE.exists():
            with open(FEEDBACK_FILE, 'w', encoding='utf-8') as f:
                f.write("# 📝 Отзывы и замечания о таймере tabatatimer.ru\n\n")
                f.write("> Этот файл автоматически собирает отзывы, замечания и предложения от подписчиков канала.\n\n")
                f.write("---\n\n")
        
        # Добавляем новый отзыв
        with open(FEEDBACK_FILE, 'a', encoding='utf-8') as f:
            f.write(f"## {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}\n\n")
            f.write(f"**Пользователь:** {комментарий.get('from_user', 'Неизвестно')}\n\n")
            f.write(f"**Сообщение:**\n> {комментарий.get('text', '')}\n\n")
            f.write(f"**Ответ пользователю:**\n> {ответ_пользователю}\n\n")
            f.write("---\n\n")
        
        print(f"✅ Отзыв сохранён в {FEEDBACK_FILE}")
        return True
    except Exception as e:
        print(f"⚠️ Ошибка сохранения отзыва: {e}")
        return False


def проанализировать_отзыв_и_предложить_решение(отзыв):
    """
    Анализирует отзыв через DeepSeek и предлагает решение/улучшение
    """
    системное_сообщение = """Аналитик UX. Проанализируй отзыв о tabatatimer.ru. Формат: 1. Проблема (кратко), 2. Приоритет (высокий/средний/низкий), 3. Решение (конкретные шаги), 4. Доп. идеи. Будь конкретным."""

    промпт = f"""Отзыв о tabatatimer.ru: "{отзыв}". Проанализируй и предложи решение."""

    try:
        response = requests.post(
            "https://api.deepseek.com/v1/chat/completions",
            headers={
                "Authorization": f"Bearer {DEEPSEEK_API_KEY}",
                "Content-Type": "application/json"
            },
            json={
                "model": "deepseek-chat",
                "messages": [
                    {"role": "system", "content": системное_сообщение},
                    {"role": "user", "content": промпт}
                ],
                "temperature": 0.7,
                "max_tokens": 350  # Оптимизировано для экономии токенов
            },
            timeout=30
        )
        
        if response.status_code == 200:
            data = response.json()
            анализ = data['choices'][0]['message']['content'].strip()
            return анализ
        else:
            print(f"❌ Ошибка DeepSeek API при анализе: {response.status_code}")
            return None
            
    except Exception as e:
        print(f"❌ Исключение при анализе отзыва: {e}")
        return None


def добавить_анализ_в_markdown(анализ):
    """Добавляет анализ от DeepSeek в markdown файл"""
    try:
        if not FEEDBACK_FILE.exists():
            return False
        
        # Читаем файл
        with open(FEEDBACK_FILE, 'r', encoding='utf-8') as f:
            содержимое = f.read()
        
        # Добавляем анализ в конец последнего отзыва
        if "---\n\n" in содержимое:
            # Находим последний разделитель и добавляем анализ перед ним
            части = содержимое.rsplit("---\n\n", 1)
            if len(частей) == 2:
                новый_контент = части[0] + f"\n**🤖 Анализ и предложения:**\n\n{анализ}\n\n---\n\n" + части[1]
                
                with open(FEEDBACK_FILE, 'w', encoding='utf-8') as f:
                    f.write(новый_контент)
                
                print("✅ Анализ добавлен в markdown")
                return True
        
        return False
    except Exception as e:
        print(f"⚠️ Ошибка добавления анализа: {e}")
        return False


def сгенерировать_ответ_на_отзыв(отзыв):
    """Генерирует ответ пользователю на отзыв о таймере"""
    системное_сообщение = """Ты - реальный человек, который ведёт фитнес-канал @fitnesstimer. Пользователь оставил отзыв о таймере.

КРИТИЧЕСКИ ВАЖНО - пиши как РЕАЛЬНЫЙ ЧЕЛОВЕК:
- Используй разговорную речь: "короче", "в общем", "спасибо", "понял", "вообще", "вроде", "как бы", "так сказать", "ну типа", "в смысле", "то есть", "короче так", "в общем-то", "в принципе да", "ну в общем", "типа того", "как-то так", "вот так вот", "ну вот", "вот так", "типа того", "как бы так", "ну как бы", "в общем как бы", "между прочим", "к слову", "кстати говоря", "к слову сказать", "кстати сказать"
- Будь эмоциональным и благодарным: "Спасибо большое!", "О, классно, что написал!", "Понял, разберёмся!", "Ого, спасибо!", "Вау, спасибо!", "О, прикольно, что написал!", "О, круто, что написал!", "О, здорово, что написал!", "О, супер, что написал!", "О, огонь, что написал!", "О, мощно, что написал!", "О, отлично, что написал!", "О, зачёт, что написал!", "О, красота, что написал!"
- Используй личные местоимения: "я", "мне", "мы", "я посмотрю", "я разберусь", "я вот", "я так", "я типа", "я в общем", "я короче", "я вообще", "я в принципе", "я вот так", "я так сделаю", "я так сделаю обычно", "я так сделаю всегда", "я так сделаю часто"
- Будь неформальным, но вежливым: "братан", "чувак", "красава", "красавчик", "молодец", "умница", "красавчик", "молодец", "умничка" (умеренно)
- Естественные конструкции: "Спасибо за фидбек!", "Понял, учту", "Разберёмся с этим", "Ну да", "Типа да", "В принципе да", "Ну как бы да", "В общем-то да", "Ну в общем да", "Типа того", "Как-то так", "Вот так вот", "Ну вот", "Вот так", "Типа того"
- Эмоциональные реакции: "ого!", "вау, спасибо!", "классно, что написал!", "круто, что написал!", "супер, что написал!", "зачёт, что написал!", "мощно, что написал!", "красота, что написал!", "здорово, что написал!", "прикольно, что написал!", "отлично, что написал!", "шикарно, что написал!", "обалденно, что написал!"

СТИЛЬ:
- Дружелюбный и благодарный
- Позитивный и конструктивный
- Короткий (100-200 символов)
- Естественный, как реальный человек

ПРИМЕРЫ:
❌ ПЛОХО: "Спасибо за отзыв. Ваше замечание принято к сведению..."
✅ ХОРОШО: "О, спасибо большое за фидбек! Понял, разберёмся с этим. Классно, что написал 💪"

❌ ПЛОХО: "Благодарим за обратную связь. Будем работать над улучшениями..."
✅ ХОРОШО: "Спасибо! Учту твоё замечание, посмотрю что можно улучшить. Огонь, что написал 🔥"

ПОМНИ: Ты НЕ бот, ты РЕАЛЬНЫЙ ЧЕЛОВЕК, который благодарит за отзыв!"""

    промпт = f"""Пользователь написал:

"{отзыв}"

Сформируй дружелюбный ответ, что отзыв принят к сведению и будет рассмотрен."""

    try:
        response = requests.post(
            "https://api.deepseek.com/v1/chat/completions",
            headers={
                "Authorization": f"Bearer {DEEPSEEK_API_KEY}",
                "Content-Type": "application/json"
            },
            json={
                "model": "deepseek-chat",
                "messages": [
                    {"role": "system", "content": системное_сообщение},
                    {"role": "user", "content": промпт}
                ],
                "temperature": 0.9,  # Высокая для естественности
                "max_tokens": 250,  # Увеличено для более естественных ответов
                "top_p": 0.9,
                "frequency_penalty": 0.2,
                "presence_penalty": 0.1
            },
            timeout=20
        )
        
        if response.status_code == 200:
            data = response.json()
            ответ = data['choices'][0]['message']['content'].strip()
            return ответ
        else:
            # Фолбэк ответ
            return "Спасибо за отзыв! Принято к сведению, будем работать над улучшениями 💪"
            
    except Exception as e:
        print(f"❌ Ошибка генерации ответа на отзыв: {e}")
        return "Спасибо за обратную связь! Принято к сведению, будем дорабатывать 🔧"


def определить_стиль_ответа(вопрос):
    """Определяет стиль ответа на основе вопроса"""
    вопрос_нижний = вопрос.lower()
    
    # Уточняющие вопросы (если вопрос короткий или неясный)
    if len(вопрос) < 30 or any(word in вопрос_нижний for word in ['что', 'какой', 'какая', 'какое']):
        if random.random() < 0.4:  # 40% вероятность уточняющего вопроса
            return 'уточняющий'
    
    # Короткие ответы (для простых вопросов)
    if len(вопрос) < 50 and any(word in вопрос_нижний for word in ['да', 'нет', 'можно', 'нужно']):
        if random.random() < 0.5:  # 50% вероятность короткого ответа
            return 'короткий'
    
    # Согласие и дополнение (если человек высказывает мнение)
    if any(word in вопрос_нижний for word in ['думаю', 'считаю', 'мне кажется', 'я думаю', 'по-моему']):
        if random.random() < 0.6:  # 60% вероятность согласия
            return 'согласие'
    
    # По умолчанию - средний развёрнутый ответ
    return 'средний'


def сгенерировать_ответ(вопрос):
    """
    Генерирует живой, мотивирующий ответ на вопрос через DeepSeek AI
    с разными стилями: короткий, средний, уточняющий, согласие
    """
    стиль = определить_стиль_ответа(вопрос)
    
    системное_сообщение = """Ты - реальный человек, который ведёт фитнес-канал @fitnesstimer. Ты не бот, а живой парень/девушка, который сам тренируется и делится опытом.

КРИТИЧЕСКИ ВАЖНО - пиши как РЕАЛЬНЫЙ ЧЕЛОВЕК:
- Используй разговорную речь: "короче", "в общем", "кстати", "короче говоря", "типа", "в принципе", "вообще", "вроде", "как бы", "так сказать", "ну типа", "в смысле", "то есть", "короче так", "в общем-то", "в принципе да", "ну в общем", "типа того", "как-то так", "вот так вот", "ну вот", "вот так", "типа того", "как бы так", "ну как бы", "в общем как бы"
- Допускай неполные предложения и разговорные конструкции: "Да, так и есть", "Ну это зависит", "В общем, да", "Ну да", "Типа да", "В принципе да", "Ну как бы да", "В общем-то да", "Ну в общем да", "Типа того", "Как-то так", "Вот так вот"
- Будь эмоциональным: "О, отличный вопрос!", "Классно, что спросил!", "Слушай, тут важно...", "Ого, хороший вопрос!", "Вау, классный вопрос!", "О, прикольно!", "О, круто!", "О, здорово!", "О, супер!", "О, огонь!", "О, мощно!", "О, отлично!", "О, зачёт!", "О, красота!"
- Используй личные местоимения: "я", "мне", "у меня", "я сам", "я лично", "я вот", "я так", "я типа", "я в общем", "я короче", "я вообще", "я в принципе"
- Добавляй личный опыт: "я сам так делал", "у меня так было", "я пробовал", "я вот так", "я так делаю", "я так делал", "я так пробовал", "я так тренируюсь", "я так занимаюсь", "я так делаю обычно", "я так делаю всегда", "я так делаю часто"
- Используй естественные паузы: "ммм", "хм", "ну", "эээ", "ааа", "ууу" (редко, для естественности)
- Будь неформальным, но не вульгарным: "чувак", "братан", "красава", "красавчик", "молодец", "умница", "красавчик", "молодец", "умничка", "красава", "красавчик" (умеренно)
- Вариативность формулировок: не повторяй одни и те же фразы
- Естественные сокращения: "инфа" вместо "информация", "фит" вместо "фитнес", "трени" вместо "тренировка", "прога" вместо "программа", "реп" вместо "повторение", "подход" вместо "подход" (редко)
- Эмоциональные реакции: "ого!", "вау!", "огонь!", "круто!", "классно!", "супер!", "зачёт!", "мощно!", "красота!", "здорово!", "прикольно!", "отлично!", "шикарно!", "обалденно!" (к месту)
- Разговорные вставки: "кстати", "между прочим", "к слову", "кстати говоря", "между прочим", "к слову сказать", "кстати сказать", "между прочим", "к слову", "кстати", "между прочим"

СТИЛЬ ОТВЕТА:
- Дружелюбный, как лучший друг в спортзале
- Энергичный, но не навязчивый
- С лёгким юмором, но по делу
- Поддерживающий и мотивирующий
- Отмечай правильность мысли человека
- Соглашайся и дополняй
- Отвечай прямо на вопрос
- Давай дополнительную инфу для размышления

ТЕХНИЧЕСКИЕ ДЕТАЛИ:
- Используй эмодзи умеренно (1-3 на ответ)
- Разнообразь длину ответов (50-500 символов)
- Шути почаще, но по делу
- Не будь занудным - пиши живо и интересно

ПРИМЕРЫ ЧЕЛОВЕЧЕСКИХ ОТВЕТОВ:
❌ ПЛОХО (как бот): "Спасибо за вопрос. Табата - это высокоинтенсивная интервальная тренировка..."
✅ ХОРОШО (как человек): "О, классный вопрос! Короче, табата - это когда ты выкладываешься на максимум 20 секунд, потом отдыхаешь 10. Я сам так тренируюсь, огонь просто 🔥"

❌ ПЛОХО: "Для начинающих рекомендуется начать с 4 раундов..."
✅ ХОРОШО: "Слушай, если ты новичок - начни с 4 раундов, не больше. Я сам так начинал, и это реально помогает войти в ритм 💪"

❌ ПЛОХО: "Да, вы правы. Это действительно эффективный метод..."
✅ ХОРОШО: "Да, точно! Я сам так думаю, это реально работает. Кстати, если добавить ещё..."

ПОМНИ: Ты НЕ бот, ты РЕАЛЬНЫЙ ЧЕЛОВЕК, который ведёт канал и отвечает на комментарии!"""

    # Получаем релевантную ссылку на SportWiki
    sportwiki_ссылка = получить_релевантную_ссылку_sportwiki(вопрос)
    sportwiki_часть = ""
    if sportwiki_ссылка:
        sportwiki_часть = f"\n\n📚 Научная информация по теме: {sportwiki_ссылка}"
    
    # Получаем случайную мотивационную фразу (с вероятностью 40%)
    фраза_часть = ""
    if random.random() < 0.4:
        фраза_часть = f"\n\n💬 {получить_случайную_фразу()}"
    
    # Получаем шутку (с вероятностью 50% - шутим почаще!)
    шутка_часть = ""
    if random.random() < 0.5:
        шутка_часть = f"\n\n😄 {получить_шутку()}"
    
    # Проверяем, нужно ли упомянуть оффер (ненавязчиво, только если релевантно)
    оффер_часть = ""
    вопрос_нижний = вопрос.lower()
    if any(word in вопрос_нижний for word in ['программа', 'курс', 'обучение', 'помощь', 'результат', 'успех']):
        оффер_часть = "\n\n🎯 Кстати, у нас есть крутые программы для достижения целей! Подробнее: https://telegra.ph/OFFER-NA-MILLIARD-07-08"
    
    # Формируем промпт в зависимости от стиля
    if стиль == 'уточняющий':
        инструкция = "Задай короткий уточняющий вопрос, чтобы лучше понять, что человек имеет в виду. Будь дружелюбным и конкретным."
        макс_символов = "50-100"
    elif стиль == 'короткий':
        инструкция = "Дай короткий, но полезный ответ. Будь конкретным и по делу."
        макс_символов = "100-150"
    elif стиль == 'согласие':
        инструкция = "Отметь правильность мысли человека, согласись с ним и дополни его мысль полезной информацией. Будь дружелюбным и поддерживающим."
        макс_символов = "200-350"
    else:  # средний
        инструкция = "Дай развёрнутый, но не занудный ответ. Отвечай прямо на вопрос, дай дополнительную инфу для размышления."
        макс_символов = "250-450"
    
    промпт = f"""Пользователь написал в комментариях к посту в фитнес-канале:

"{вопрос}"

СТИЛЬ ОТВЕТА: {стиль}
ИНСТРУКЦИЯ: {инструкция}
МАКСИМАЛЬНАЯ ДЛИНА: {макс_символов} символов

КРИТИЧЕСКИ ВАЖНО - ПИШИ КАК РЕАЛЬНЫЙ ЧЕЛОВЕК:
1. Начни с эмоциональной реакции: "О, классный вопрос!", "Слушай, тут важно...", "Да, хороший момент!", "Ого, хороший вопрос!", "Вау, классный вопрос!", "О, прикольно!", "О, круто!", "О, здорово!", "О, супер!", "О, огонь!", "О, мощно!", "О, отлично!", "О, зачёт!", "О, красота!"
2. Используй разговорную речь: "короче", "в общем", "кстати", "типа", "в принципе", "вообще", "вроде", "как бы", "так сказать", "ну типа", "в смысле", "то есть", "короче так", "в общем-то", "в принципе да", "ну в общем", "типа того", "как-то так", "вот так вот", "ну вот", "вот так", "типа того", "как бы так", "ну как бы", "в общем как бы", "между прочим", "к слову", "кстати говоря", "к слову сказать", "кстати сказать"
3. Добавь личный опыт: "я сам так делал", "у меня так было", "я пробовал", "я вот так", "я так делаю", "я так делал", "я так пробовал", "я так тренируюсь", "я так занимаюсь", "я так делаю обычно", "я так делаю всегда", "я так делаю часто", "я лично так", "я вот так делаю", "я так делаю обычно"
4. Будь неформальным: "чувак", "братан", "красава", "красавчик", "молодец", "умница", "красавчик", "молодец", "умничка" (умеренно, если уместно)
5. Используй естественные конструкции: "Ну это зависит от...", "В общем, да", "Короче говоря...", "Ну да", "Типа да", "В принципе да", "Ну как бы да", "В общем-то да", "Ну в общем да", "Типа того", "Как-то так", "Вот так вот", "Ну вот", "Вот так", "Типа того"
6. Вариативность: не повторяй одни и те же фразы, которые ты использовал в предыдущих ответах
7. Эмоциональность: "огонь!", "вау!", "классно!", "круто!", "супер!", "зачёт!", "мощно!", "красота!", "здорово!", "прикольно!", "отлично!", "шикарно!", "обалденно!" (к месту, не переборщи)

ОБЯЗАТЕЛЬНО:
- Отмечай правильность мысли человека, если он прав ("Да, точно!", "Согласен!", "Именно так!")
- Отвечай прямо на поставленный вопрос
- Давай дополнительную инфу для размышления
- Шути почаще (лёгкий юмор, по делу)
- Если вопрос про таймер tabatatimer.ru - обязательно упомяни ссылку https://tabatatimer.ru/#timer
- Будь энергичным и позитивным
- Если человек сомневается - подбодри его!

{sportwiki_часть}{фраза_часть}{шутка_часть}{оффер_часть}

ПОМНИ: Ты НЕ бот, ты РЕАЛЬНЫЙ ЧЕЛОВЕК! Пиши так, как бы ты написал другу в личке - живо, естественно, с эмоциями, но по делу."""

    try:
        response = requests.post(
            "https://api.deepseek.com/v1/chat/completions",
            headers={
                "Authorization": f"Bearer {DEEPSEEK_API_KEY}",
                "Content-Type": "application/json"
            },
            json={
                "model": "deepseek-chat",
                "messages": [
                    {"role": "system", "content": системное_сообщение},
                    {"role": "user", "content": промпт}
                ],
                "temperature": 0.95,  # Очень высокая для максимальной естественности и разнообразия
                "max_tokens": 400,  # Увеличено для более развёрнутых и естественных ответов
                "top_p": 0.9,  # Для более разнообразных ответов
                "frequency_penalty": 0.3,  # Штрафуем повторения для большей вариативности
                "presence_penalty": 0.2  # Поощряем новые темы и идеи
            },
            timeout=20
        )
        
        if response.status_code == 200:
            data = response.json()
            ответ = data['choices'][0]['message']['content'].strip()
            
            # Ограничиваем длину ответа (Telegram лимит для одного сообщения ~4096 символов)
            # Но для комментариев лучше короче - до 600 символов (с учётом эмодзи и форматирования)
            if len(ответ) > 600:
                ответ = ответ[:597] + "..."
            
            return ответ
        else:
            print(f"❌ Ошибка DeepSeek API: {response.status_code}")
            return None
            
    except Exception as e:
        print(f"❌ Исключение при генерации ответа: {e}")
        return None


def получить_message_thread_id_из_комментария(chat_id_группы, message_id):
    """
    Пытается получить message_thread_id из комментария через API Telegram.
    Для комментариев к постам в каналах с форумами это может быть необходимо.
    """
    try:
        # Получаем информацию о сообщении через getUpdates или forwardMessage
        # Но проще всего - попробовать получить через форвард сообщения
        # Или использовать метод getChat для получения информации о группе
        
        # Альтернативный подход: если комментарий приходит из группы обсуждений,
        # но не имеет reply_to_message, возможно, нужно получить информацию о связанном посте
        # через метод getChat или другой API
        
        # Пока возвращаем None - это будет обработано в основной функции
        return None
    except Exception as e:
        print(f"⚠️ Ошибка получения message_thread_id: {e}")
        return None


def отправить_ответ(message_id, ответ, chat_id_группы, message_thread_id=None, is_comment_to_post=False):
    """
    Отправляет ответ на комментарий в Telegram
    
    Args:
        message_id: ID сообщения, на которое отвечаем
        ответ: Текст ответа
        chat_id_группы: ID группы обсуждений (НЕ канала!)
        message_thread_id: ID топика (topic) для каналов с форумами (опционально)
        is_comment_to_post: Флаг, что это комментарий к посту (для каналов с форумами)
    """
    url = f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/sendMessage"
    
    # Форматируем ответ: убираем лишний префикс, если он уже есть
    if ответ.startswith("💬"):
        текст_ответа = ответ
    else:
        текст_ответа = f"💬 {ответ}"
    
    # КРИТИЧЕСКИ ВАЖНО: Для каналов с форумами (topics) ответы нужно отправлять
    # НАПРЯМУЮ В КАНАЛ с указанием message_thread_id исходного поста,
    # а НЕ в группу обсуждений!
    
    # Пробуем сначала отправить в группу обсуждений (обычный случай)
    params = {
        'chat_id': chat_id_группы,  # ID группы обсуждений
        'text': текст_ответа,
        'reply_to_message_id': message_id,
        'parse_mode': 'HTML',
        'disable_web_page_preview': False
    }
    
    if message_thread_id:
        params['message_thread_id'] = message_thread_id
        print(f"📌 Используем message_thread_id: {message_thread_id}")
    
    try:
        response = requests.post(url, json=params, timeout=10)
        if response.status_code == 200:
            print(f"✅ Ответ успешно отправлен в группу обсуждений!")
            return True
        else:
            error_data = response.json() if response.text else {}
            error_description = error_data.get('description', response.text)
            print(f"❌ Ошибка отправки ответа: {response.status_code}")
            print(f"Ответ API: {error_description}")
            print(f"🔍 Параметры запроса: chat_id={chat_id_группы}, message_id={message_id}, thread_id={message_thread_id}")
            
            # ЕСЛИ ошибка "channel direct messages topic must be specified",
            # это значит, что канал использует форумы (topics)
            # В этом случае нужно отправить ответ НАПРЯМУЮ В КАНАЛ с message_thread_id
            if "topic must be specified" in error_description.lower():
                print(f"⚠️ Обнаружена ошибка 'topic must be specified' - канал использует форумы!")
                print(f"🔄 Пробуем отправить ответ НАПРЯМУЮ В КАНАЛ...")
                
                # Для каналов с форумами нужно:
                # 1. Отправить в канал (TELEGRAM_CHAT_ID), а не в группу обсуждений
                # 2. Указать message_thread_id исходного поста
                # 3. НЕ использовать reply_to_message_id (это для комментариев в группе)
                
                # Пробуем получить message_thread_id из исходного поста
                # Если его нет, пробуем отправить без него (может сработать для некоторых случаев)
                
                params_channel = {
                    'chat_id': TELEGRAM_CHAT_ID,  # Отправляем в канал!
                    'text': текст_ответа,
                    'parse_mode': 'HTML',
                    'disable_web_page_preview': False
                }
                
                # Если есть message_thread_id - используем его
                if message_thread_id:
                    params_channel['message_thread_id'] = message_thread_id
                    print(f"📌 Отправляем в канал с message_thread_id: {message_thread_id}")
                else:
                    print(f"⚠️ message_thread_id не найден, пробуем отправить в канал без него...")
                
                retry_response = requests.post(url, json=params_channel, timeout=10)
                if retry_response.status_code == 200:
                    print(f"✅ Ответ успешно отправлен в канал!")
                    return True
                else:
                    retry_error = retry_response.json() if retry_response.text else {}
                    retry_error_desc = retry_error.get('description', retry_response.text)
                    print(f"❌ Отправка в канал также не удалась: {retry_response.status_code}")
                    print(f"Ответ: {retry_error_desc}")
                    print(f"💡 Возможно, нужно получить message_thread_id из исходного поста через API")
            
            return False
    except Exception as e:
        print(f"❌ Исключение при отправке ответа: {e}")
        return False


def главная():
    """
    Главная функция: обрабатывает новые комментарии и отвечает на них
    """
    if not all([TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, DEEPSEEK_API_KEY]):
        print("❌ Не заданы необходимые переменные окружения")
        print("Нужны: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, DEEPSEEK_API_KEY")
        return False
    
    print("🤖 Запуск обработки комментариев...")
    print(f"⏰ Время запуска: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
    print(f"🔑 TELEGRAM_CHAT_ID: {TELEGRAM_CHAT_ID}")
    print(f"🔑 TELEGRAM_BOT_TOKEN: {'*' * 10 if TELEGRAM_BOT_TOKEN else 'НЕ ЗАДАН'}")
    print(f"🔑 DEEPSEEK_API_KEY: {'*' * 10 if DEEPSEEK_API_KEY else 'НЕ ЗАДАН'}")
    
    # Загружаем состояние
    last_update_id = загрузить_состояние()
    print(f"📌 Последний обработанный update_id: {last_update_id}")
    
    # Получаем новые комментарии
    print("🔍 Получаем обновления из Telegram...")
    комментарии, новый_update_id = получить_новые_комментарии(last_update_id)
    print(f"📊 Получено обновлений: новый_update_id={новый_update_id}, найдено комментариев: {len(комментарии)}")
    
    # Детальное логирование найденных комментариев
    if комментарии:
        print(f"\n📋 Найденные комментарии:")
        for idx, коммент in enumerate(комментарии, 1):
            print(f"  {idx}. От: {коммент.get('from_user')} (@{коммент.get('username', 'N/A')})")
            print(f"     Текст: {коммент.get('text', '')[:100]}...")
            print(f"     Chat ID: {коммент.get('chat_id')}")
            print(f"     Message ID: {коммент.get('message_id')}")
    else:
        print("ℹ️ Комментарии не найдены. Возможные причины:")
        print("  - Нет новых комментариев с момента последней проверки")
        print("  - Комментарии не прошли фильтрацию (см. логи выше)")
        print("  - Бот не имеет доступа к группе обсуждений")
        print("  - TELEGRAM_CHAT_ID настроен неправильно")
    
    if not комментарии:
        print("✅ Новых комментариев нет")
        if новый_update_id > last_update_id:
            сохранить_состояние(новый_update_id)
            print(f"💾 Состояние обновлено: {новый_update_id}")
        return True
    
    print(f"📨 Найдено комментариев: {len(комментарии)}")
    
    обработано = 0
    отвечено = 0
    отзывов_собрано = 0
    
    for комментарий in комментарии:
        обработано += 1
        текст = комментарий['text']
        message_id = комментарий['message_id']
        пользователь = комментарий['from_user']
        
        print(f"\n💬 Комментарий от {пользователь}: {текст[:50]}...")
        
        # КРИТИЧЕСКИ ВАЖНО: Проверяем, не отвечали ли мы уже на этот комментарий
        if уже_отвечено(message_id):
            print(f"⏭️ Пропускаем: уже отвечали на этот комментарий (message_id={message_id})")
            continue
        
        # ПРОВЕРКА: является ли это отзывом/вопросом про таймер/сайт?
        if это_отзыв_про_таймер(текст):
            print("🎯 Обнаружен отзыв/замечание про таймер/сайт!")
            
            # Добавляем в статистику как отзыв
            if STATISTICS_AVAILABLE:
                добавить_комментарий(комментарий, тип='feedback')
                print("📊 Отзыв добавлен в статистику")
            
            # Генерируем ответ пользователю
            ответ_пользователю = сгенерировать_ответ_на_отзыв(текст)
            
            # Сохраняем отзыв в markdown
            if сохранить_отзыв_в_markdown(комментарий, ответ_пользователю):
                отзывов_собрано += 1
                
                # Анализируем отзыв через DeepSeek и предлагаем решение
                print("🤖 Анализируем отзыв и предлагаем решение...")
                анализ = проанализировать_отзыв_и_предложить_решение(текст)
                
                if анализ:
                    добавить_анализ_в_markdown(анализ)
                    print("✅ Анализ добавлен в markdown")
            
            # Получаем chat_id группы обсуждений и message_thread_id из комментария
            chat_id_группы = комментарий.get('chat_id')
            message_thread_id = комментарий.get('message_thread_id')
            is_comment_to_post = комментарий.get('is_comment', False)
            if not chat_id_группы:
                print("❌ Не найден chat_id группы обсуждений для отзыва")
                continue
            
            # Отправляем ответ пользователю в группу обсуждений
            if отправить_ответ(message_id, ответ_пользователю, chat_id_группы, message_thread_id, is_comment_to_post):
                отвечено += 1
                # Сохраняем message_id как отвеченный
                сохранить_отвеченное_сообщение(message_id)
                print("✅ Ответ пользователю отправлен!")
            else:
                print("❌ Не удалось отправить ответ")
            
            continue
        
        # Обычная обработка комментариев (не про таймер) - вопросы о тренировках
        # Добавляем в статистику как вопрос о тренировках
        if STATISTICS_AVAILABLE:
            добавить_комментарий(комментарий, тип='question')
            print("📊 Вопрос добавлен в статистику")
        
        # Проверяем, нужно ли отвечать
        if not нужно_отвечать(текст):
            print("⏭️ Пропускаем (не вопрос или не релевантно)")
            continue
        
        print("🤖 Генерируем ответ через DeepSeek...")
        
        # Генерируем ответ
        ответ = сгенерировать_ответ(текст)
        
        if not ответ:
            print("❌ Не удалось сгенерировать ответ")
            continue
        
        print(f"✅ Ответ сгенерирован: {ответ[:50]}...")
        
        # Получаем chat_id группы обсуждений и message_thread_id из комментария
        chat_id_группы = комментарий.get('chat_id')
        message_thread_id = комментарий.get('message_thread_id')
        is_comment_to_post = комментарий.get('is_comment', False)
        if not chat_id_группы:
            print("❌ Не найден chat_id группы обсуждений")
            continue
        
        # Отправляем ответ в группу обсуждений
        if отправить_ответ(message_id, ответ, chat_id_группы, message_thread_id, is_comment_to_post):
            отвечено += 1
            # Сохраняем message_id как отвеченный
            сохранить_отвеченное_сообщение(message_id)
            print("✅ Ответ отправлен!")
        else:
            print("❌ Не удалось отправить ответ")
    
    # Сохраняем новое состояние
    if новый_update_id > last_update_id:
        сохранить_состояние(новый_update_id)
    
    print(f"\n📊 Итого: обработано {обработано}, отвечено {отвечено}, отзывов собрано {отзывов_собрано}")
    
    if отзывов_собрано > 0:
        print(f"\n📝 Отзывы сохранены в {FEEDBACK_FILE}")
        print("💡 Просмотрите файл для анализа и предложений по улучшению!")
    
    # Отправляем summary отчёт, если есть новая активность
    if STATISTICS_AVAILABLE and (обработано > 0 or отзывов_собрано > 0):
        # Отправляем summary раз в день (проверяем по времени последней отправки)
        # Для простоты отправляем каждый раз, когда есть активность
        # В production можно добавить проверку времени последней отправки
        try:
            print("\n📧 Генерируем и отправляем summary отчёт...")
            telegram_chat_id = os.getenv('ADMIN_TELEGRAM_CHAT_ID')
            отправить_summary(
                период_дней=7,
                отправлять_email=True,
                отправлять_telegram=bool(telegram_chat_id),
                telegram_chat_id=telegram_chat_id
            )
            print("✅ Summary отчёт отправлен")
        except Exception as e:
            print(f"⚠️ Ошибка отправки summary отчёта: {e}")
    
    return True


if __name__ == "__main__":
    успех = главная()
    exit(0 if успех else 1)

