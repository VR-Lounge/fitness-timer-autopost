# ✅ Упрощение проверки уникальности для skinnyms.com

## 🎯 Цель

Упростить проверку уникальности, чтобы статьи из skinnyms.com публиковались успешно без блокировок из-за дубликатов изображений.

## ✅ Внесённые изменения

### 1. Упрощённая проверка для skinnyms.com

**Файл:** `content_uniqueness.py`

**Изменения:**
- Для статей из `skinnyms.com` проверяется **ТОЛЬКО URL источника** (чтобы не публиковать одну и ту же статью дважды)
- **Пропускаются проверки:**
  - ❌ Уникальность изображений
  - ❌ Уникальность текста
  - ❌ Уникальность пары ФОТО+ТЕКСТ
  - ❌ Семантическая схожесть через DeepSeek
  - ❌ Схожесть заголовков
  - ❌ Проверка по персонажам и темам

**Логика:**
```python
if url_статьи and 'skinnyms.com' in url_статьи.lower():
    # Проверяем только URL источника
    # Если статья с этого URL уже опубликована - блокируем
    # Иначе - разрешаем публикацию
```

### 2. Хеши не сохраняются для skinnyms.com

**Файл:** `content_uniqueness.py` → функция `сохранить_контент_как_использованный()`

**Изменения:**
- Для статей из `skinnyms.com` хеши **НЕ сохраняются**
- Это позволяет использовать одни и те же изображения в разных статьях
- Изображения из skinnyms.com могут использоваться повторно

### 3. Обновлены вызовы функции

**Файлы:**
- `womenshealth_parser.py`
- `menshealth_parser.py`

**Изменения:**
- Добавлен параметр `url_статьи` в вызов `сохранить_контент_как_использованный()`

## 📋 Результат

### ✅ Для skinnyms.com:
- Проверяется только уникальность URL источника
- Изображения могут использоваться повторно
- Текст может быть похожим (не блокируется)
- Статьи считаются 100% релевантными

### ✅ Для других источников:
- Полная проверка уникальности (как было)
- Проверка изображений, текста, семантической схожести
- Хеши сохраняются

## 🧪 Тестирование

После изменений статьи из skinnyms.com должны:
1. ✅ Публиковаться в Telegram
2. ✅ Сохраняться в blog-posts.json
3. ✅ Создавать HTML страницы
4. ✅ Не блокироваться из-за дубликатов изображений

## 📝 Примечания

- Статьи из skinnyms.com считаются **100% релевантными**
- Проверка URL источника предотвращает публикацию одной и той же статьи дважды
- Изображения могут использоваться в разных статьях (это нормально для skinnyms.com)
