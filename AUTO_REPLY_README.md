# 🔄 Автоматический ответ на комментарии

Система автоматического анализа и ответа на комментарии в Telegram канале через DeepSeek AI.

## 🎯 Возможности

- ✅ Автоматическое получение новых комментариев из канала
- ✅ AI-анализ вопроса через DeepSeek
- ✅ Умная фильтрация (вопросы, спам, релевантность)
- ✅ Автоматическая отправка ответов
- ✅ Сохранение состояния (не отвечает дважды на один комментарий)
- ✨ **Живой, дружелюбный стиль общения** (как лучший друг в спортзале)
- ✨ **Мотивационные цитаты** от известных атлетов (Арнольд, Брюс Ли и др.)
- ✨ **Научные обоснования** через ссылки на [SportWiki](https://sportwiki.to/)
- ✨ **Ненавязчивое упоминание офферов** при релевантных вопросах

## ⚙️ Настройка

### 1. Права бота в Telegram

**Важно:** Бот должен быть администратором канала с правами:
- ✅ Чтение сообщений
- ✅ Отправка сообщений
- ✅ Ответ на сообщения

**КРИТИЧЕСКИ ВАЖНО для комментариев к постам:**
- ✅ У канала должна быть **включена группа обсуждений** (Discussion Group)
- ✅ Бот должен быть **администратором группы обсуждений** с правами:
  - Чтение сообщений
  - Отправка сообщений
  - Ответ на сообщения

**Как проверить:**
1. Откройте настройки канала → Обсуждения
2. Убедитесь, что группа обсуждений подключена
3. Добавьте бота в группу обсуждений как администратора

### 2. Переменные окружения

Убедитесь, что в GitHub Secrets настроены:
- `TELEGRAM_BOT_TOKEN` — токен бота
- `TELEGRAM_CHAT_ID` — ID канала (тот же, что для автопостинга)
- `DEEPSEEK_API_KEY` — ключ DeepSeek API

### 3. Активация workflow

Workflow запускается автоматически **каждые 30 минут** (оптимизировано) или вручную через GitHub Actions UI.

## 🔧 Как это работает

1. **Получение комментариев**
   - Скрипт использует `getUpdates` API Telegram
   - Получает комментарии из канала и группы обсуждений
   - Определяет комментарии к постам (через reply_to_message или тип чата)
   - Получает только новые комментарии (начиная с последнего обработанного)

2. **Фильтрация**
   - Проверяет, является ли текст вопросом (есть `?` или ключевые слова)
   - Исключает спам (ссылки, реклама)
   - Проверяет релевантность (ключевые слова: таймер, тренировка, рецепт и т.д.)

3. **Генерация ответа**
   - Использует DeepSeek AI для анализа вопроса
   - Генерирует живой, мотивирующий ответ (до 500 символов)
   - Подкрепляет советы научными фактами через ссылки на SportWiki
   - Вставляет мотивационные цитаты (с вероятностью 30%)
   - Ненавязчиво упоминает офферы при релевантных вопросах
   - Упоминает ссылку на таймер, если вопрос про него

4. **Отправка ответа**
   - Отвечает на комментарий через `reply_to_message_id`
   - Сохраняет состояние, чтобы не отвечать дважды

## 📝 Настройка фильтров

В файле `auto_reply.py` можно настроить:

### Ключевые слова вопросов
```python
QUESTION_KEYWORDS = [
    'как', 'что', 'почему', 'где', 'когда', 'сколько', ...
]
```

### Триггеры для автоматического ответа
```python
AUTO_REPLY_TRIGGERS = [
    'таймер', 'tabata', 'hiit', 'тренировка', ...
]
```

### Слова-спам (игнорируются)
```python
SPAM_KEYWORDS = [
    'купить', 'продать', 'реклама', 'ссылка', ...
]
```

## 🧪 Тестирование

### Локальный запуск

```bash
export TELEGRAM_BOT_TOKEN="your_token"
export TELEGRAM_CHAT_ID="your_chat_id"
export DEEPSEEK_API_KEY="your_key"

python3 auto_reply.py
```

### Ручной запуск через GitHub Actions

1. Перейдите в **Actions** → **🔄 Автоматический ответ на комментарии**
2. Нажмите **Run workflow**
3. Выберите ветку и запустите

## 📊 Логирование

Скрипт выводит подробные логи:
- ✅ Успешные операции
- ⚠️ Предупреждения
- ❌ Ошибки

Пример вывода:
```
🤖 Запуск обработки комментариев...
📌 Последний обработанный update_id: 12345
📨 Найдено комментариев: 2

💬 Комментарий от Иван: Как начать тренироваться?
🤖 Генерируем ответ через DeepSeek...
✅ Ответ сгенерирован: Начните с простых упражнений...
✅ Ответ отправлен!

📊 Итого: обработано 2, отвечено 1
```

## ⚠️ Ограничения

1. **Telegram API**
   - `getUpdates` работает только для ботов (не для каналов напрямую)
   - Комментарии в каналах доступны только через бота-администратора

2. **Частота запросов**
   - GitHub Actions запускается каждые 15 минут
   - DeepSeek API имеет лимиты на количество запросов

3. **Качество ответов**
   - Зависит от качества вопроса пользователя
   - Может потребоваться доработка промптов для DeepSeek

## 🔄 Обновление состояния

Состояние сохраняется в файл `.auto_reply_state.json`:
```json
{
  "last_update_id": 12345
}
```

Этот файл **не коммитится** в репозиторий (добавлен в `.gitignore`).

## 💡 Рекомендации

1. **Мониторинг**
   - Периодически проверяйте логи в GitHub Actions
   - Следите за качеством ответов

2. **Доработка промптов**
   - Настройте системное сообщение для DeepSeek под ваш стиль
   - Добавьте больше контекста о канале и тематике

3. **Расширение фильтров**
   - Добавьте больше ключевых слов в `AUTO_REPLY_TRIGGERS`
   - Настройте `SPAM_KEYWORDS` под ваш канал

## 🐛 Решение проблем

### Бот не получает комментарии

1. Проверьте, что бот — администратор канала
2. Убедитесь, что `TELEGRAM_CHAT_ID` указан правильно
3. Проверьте права бота в настройках канала

### Не генерируются ответы

1. Проверьте `DEEPSEEK_API_KEY` в Secrets
2. Убедитесь, что есть баланс на DeepSeek API
3. Проверьте логи в GitHub Actions

### Отвечает на все комментарии

1. Настройте фильтры в `нужно_отвечать()`
2. Добавьте больше условий для проверки релевантности
3. Увеличьте `MIN_QUESTION_LENGTH`

## 📚 Дополнительно

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [DeepSeek API Documentation](https://platform.deepseek.com/api-docs/)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)

