name: 🔥 Автопостинг FITNESS TIMER

# Запуск workflow
on:
  schedule:
    # Каждый день в 9:00 МСК (6:00 UTC)
    - cron: '0 6 * * *'
        # Каждый день в 17:00 МСК (14:00 UTC)
            - cron: '0 14 * * *'
  # Возможность запустить вручную
  workflow_dispatch:

jobs:
  autopost:
    name: Генерация и отправка поста
    runs-on: ubuntu-latest
    
    steps:
      # Шаг 1: Клонирование репозитория
      - name: 📥 Клонирование репозитория
        uses: actions/checkout@v4
      
      # Шаг 2: Установка Python
      - name: 🐍 Установка Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      # Шаг 3: Установка зависимостей
      - name: 📦 Установка зависимостей
        run: |
          pip install requests
      
      # Шаг 4: Запуск скрипта автопостинга
      - name: 🚀 Запуск автопостинга
        env:
          DEEPSEEK_API_KEY: \${{ secrets.DEEPSEEK_API_KEY }}
          TELEGRAM_BOT_TOKEN: \${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: \${{ secrets.TELEGRAM_CHAT_ID }}
        run: python автопостинг.py
      
      # Шаг 5: Уведомление об успехе
      - name: ✅ Готово
        if: success()
        run: echo "Пост успешно отправлен в канал @fitnesstimer!"
