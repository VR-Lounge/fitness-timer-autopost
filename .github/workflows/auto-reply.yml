name: 🔄 Автоматический ответ на комментарии

on:
  schedule:
    # Каждые 30 минут проверяем новые комментарии (оптимизировано для 2000 мин/месяц)
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  auto-reply:
    name: Обработка комментариев и ответы
    runs-on: ubuntu-latest
    
    steps:
      - name: Клонирование репозитория
        uses: actions/checkout@v4
      
      - name: Установка Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'  # Кэшируем pip для ускорения
      
      - name: Установка зависимостей
        run: |
          pip install --upgrade pip --quiet
          pip install requests --quiet
      
      - name: Обработка комментариев и отправка ответов
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          python auto_reply.py
      
      # Убрали лишний шаг для оптимизации времени выполнения

