name: 🔄 Автоматический ответ на комментарии

on:
  schedule:
    # Проверяем новые комментарии 2 раза в день (утром и вечером) для оптимизации использования GitHub Actions
    - cron: '0 8 * * *'   # 08:00 UTC (11:00 МСК) - утро
    - cron: '0 20 * * *'  # 20:00 UTC (23:00 МСК) - вечер
  workflow_dispatch:

jobs:
  auto-reply:
    name: Обработка комментариев и ответы
    runs-on: ubuntu-latest
    
    steps:
      - name: Клонирование репозитория
        uses: actions/checkout@v4
      
      - name: Установка Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'  # Кэшируем pip для ускорения
      
      - name: Получение ID последнего успешного запуска
        id: get-last-run
        run: |
          echo "🔍 Ищем последний успешный запуск workflow..."
          WORKFLOW_ID=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/auto-reply.yml" | \
            jq -r '.id')
          
          if [ -z "$WORKFLOW_ID" ] || [ "$WORKFLOW_ID" = "null" ]; then
            echo "⚠️ Не удалось получить ID workflow"
            echo "run_id=" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          LAST_RUN=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/$WORKFLOW_ID/runs?per_page=2&status=success" | \
            jq -r '.workflow_runs[] | select(.id != ${{ github.run_id }}) | .id' | head -1)
          
          if [ -z "$LAST_RUN" ] || [ "$LAST_RUN" = "null" ]; then
            echo "⚠️ Предыдущий успешный запуск не найден (первый запуск)"
            echo "run_id=" >> $GITHUB_OUTPUT
          else
            echo "✅ Найден предыдущий успешный запуск: $LAST_RUN"
            echo "run_id=$LAST_RUN" >> $GITHUB_OUTPUT
          fi
      
      - name: Загрузка состояния (artifacts)
        uses: actions/download-artifact@v4
        if: steps.get-last-run.outputs.run_id != ''
        with:
          name: auto-reply-state
          path: ./state
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ steps.get-last-run.outputs.run_id }}
        continue-on-error: true
      
      - name: Перемещение файлов состояния
        if: steps.get-last-run.outputs.run_id != ''
        run: |
          echo "📁 Проверяем директорию state..."
          if [ -d "./state" ]; then
            echo "✅ Найдена директория state"
            ls -la ./state/
            if [ -f "./state/.auto_reply_state.json" ]; then
              cp "./state/.auto_reply_state.json" ".auto_reply_state.json"
              echo "✅ Скопирован .auto_reply_state.json"
              cat .auto_reply_state.json
            else
              echo "⚠️ Файл ./state/.auto_reply_state.json не найден"
            fi
            if [ -f "./state/.answered_messages.json" ]; then
              cp "./state/.answered_messages.json" ".answered_messages.json"
              echo "✅ Скопирован .answered_messages.json"
            else
              echo "⚠️ Файл ./state/.answered_messages.json не найден"
            fi
          else
            echo "⚠️ Директория ./state не найдена"
          fi
      
      - name: Проверка загруженного состояния
        run: |
          echo "🔍 Проверка состояния после загрузки artifacts..."
          if [ -f ".auto_reply_state.json" ]; then
            echo "✅ Файл .auto_reply_state.json найден"
            echo "📄 Содержимое:"
            cat .auto_reply_state.json
          else
            echo "⚠️ Файл .auto_reply_state.json не найден (первый запуск или artifacts не найдены)"
            echo "📝 Создаём пустой файл для первого запуска..."
            echo '{"last_update_id": 0}' > .auto_reply_state.json
          fi
          if [ -f ".answered_messages.json" ]; then
            echo "✅ Файл .answered_messages.json найден"
            echo "📊 Размер: $(wc -l < .answered_messages.json) строк"
          else
            echo "⚠️ Файл .answered_messages.json не найден"
            echo "📝 Создаём пустой файл..."
            echo '[]' > .answered_messages.json
          fi
      
      - name: Установка зависимостей
        run: |
          pip install --upgrade pip --quiet
          pip install requests --quiet
          # Для отправки email отчётов (встроено в Python, но убедимся)
          python -c "import smtplib; import email" || echo "Email модули доступны"
      
      - name: Обработка комментариев и отправка ответов
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          ADMIN_TELEGRAM_CHAT_ID: ${{ secrets.ADMIN_TELEGRAM_CHAT_ID }}
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
        run: |
          python auto_reply.py
      
      - name: Проверка состояния перед сохранением
        if: always()
        run: |
          echo "🔍 Проверка состояния перед сохранением в artifacts..."
          echo "📁 Текущая директория: $(pwd)"
          echo "📁 Содержимое директории:"
          ls -la | grep -E "(auto_reply|answered_messages)" || echo "Файлы не найдены"
          echo ""
          if [ -f ".auto_reply_state.json" ]; then
            echo "✅ Файл .auto_reply_state.json существует"
            echo "📄 Содержимое:"
            cat .auto_reply_state.json
          else
            echo "❌ Файл .auto_reply_state.json НЕ существует!"
            echo "📝 Создаём файл с дефолтным значением..."
            echo '{"last_update_id": 0}' > .auto_reply_state.json
            echo "✅ Файл создан"
          fi
          if [ -f ".answered_messages.json" ]; then
            echo "✅ Файл .answered_messages.json существует"
            echo "📊 Размер: $(wc -l < .answered_messages.json) строк"
          else
            echo "⚠️ Файл .answered_messages.json не существует"
            echo "📝 Создаём пустой файл..."
            echo '[]' > .answered_messages.json
            echo "✅ Файл создан"
          fi
          echo ""
          echo "📁 Финальная проверка файлов:"
          ls -la .auto_reply_state.json .answered_messages.json 2>&1 || echo "Ошибка проверки"
      
      - name: Сохранение состояния (artifacts)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: auto-reply-state
          path: |
            .auto_reply_state.json
            .answered_messages.json
          retention-days: 90
          if-no-files-found: error
          include-hidden-files: true

