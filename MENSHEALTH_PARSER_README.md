# 📰 Парсер Men's Health с рерайтингом

Автоматический парсер статей из Men's Health с фильтрацией по темам, рерайтингом через DeepSeek AI и публикацией в Telegram канал.

## 🎯 Функционал

- ✅ **Парсинг RSS фидов** Men's Health (workouts, nutrition, fitness, cardio)
- ✅ **Умная фильтрация** по ключевым словам (TABATA, HIIT, AMRAP, EMOM, интервальные тренировки, диеты)
- ✅ **Парсинг полного текста** статей с сайта menshealth.com
- ✅ **Парсинг изображений** прямо с сайта (og:image + изображения из статьи)
- ✅ **Качественный рерайтинг** через DeepSeek AI (убирает все следы источника)
- ✅ **Публикация в Telegram** канал с текстом и фото
- ✅ **Защита от дублирования** (хранит список обработанных статей)

## 📋 Требования

### Переменные окружения (GitHub Secrets):

- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `TELEGRAM_CHAT_ID` - ID канала для публикации
- `DEEPSEEK_API_KEY` - API ключ DeepSeek для рерайтинга

### Зависимости:

```bash
pip install -r requirements.txt
```

Включает:
- `requests` - для HTTP запросов
- `beautifulsoup4` - для парсинга HTML
- `lxml` - парсер для BeautifulSoup

## 🚀 Использование

### Локальный запуск:

```bash
cd fitness-timer-autopost
export TELEGRAM_BOT_TOKEN="your_token"
export TELEGRAM_CHAT_ID="your_chat_id"
export DEEPSEEK_API_KEY="your_api_key"
python3 menshealth_parser.py
```

### Автоматический запуск (GitHub Actions):

Workflow запускается автоматически:
- **08:00 UTC** (11:00 МСК) - утренний запуск
- **20:00 UTC** (23:00 МСК) - вечерний запуск

Или вручную через `workflow_dispatch`.

## 🔍 Как работает фильтрация

Статья считается релевантной, если содержит **минимум 2 ключевых слова** из списка:

### Интервальные тренировки:
- tabata, hiit, amrap, emom
- interval training, interval workout
- high intensity, circuit training
- timed workout, workout timer

### Диеты и питание:
- diet, nutrition, meal plan
- protein, carb, calorie
- weight loss, fat loss, metabolism

### Упражнения:
- workout, exercise, training
- cardio, strength, endurance
- quick workout, home workout

## 🤖 Рерайтинг через DeepSeek

Система автоматически:
1. Извлекает полный текст статьи
2. Отправляет в DeepSeek AI с промптом для рерайтинга
3. Получает уникальный текст без следов источника
4. Адаптирует для русскоязычной аудитории tabatatimer.ru
5. Добавляет упоминания о табата таймере где уместно

### Промпт для рерайтинга:

- Полностью переписывает текст своими словами
- Убирает упоминания Men's Health и авторов
- Использует разговорный стиль
- Добавляет эмодзи для визуального оформления
- Длина: 800-1200 слов
- Адаптирован для tabatatimer.ru

## 📸 Парсинг изображений

Система извлекает изображения:
1. **og:image** из meta-тегов (главное изображение)
2. **Изображения из статьи** (первые 10, затем фильтрует)
3. **Фильтрация**:
   - Минимальный размер: 200x200px
   - Только JPG, PNG, WebP
   - Удаление дубликатов
4. **Выбор**: первые 3 лучших изображения

## 📝 Формат поста в Telegram

```
[Рерайт статьи]

💪 Используй наш табата таймер для интервальных тренировок: https://www.tabatatimer.ru

#Табата #HIIT #Тренировки #Фитнес
```

Пост отправляется с **фото** (если найдено) или **только текст**.

## 💾 Хранение состояния

Система хранит список обработанных статей в файле `.menshealth_processed.json`:
- Сохраняет URL обработанных статей
- Ограничение: последние 1000 статей
- Автоматически сохраняется в GitHub Actions artifacts

## ⚙️ Настройка

### Изменение расписания:

Отредактируйте `.github/workflows/menshealth-parser.yml`:

```yaml
schedule:
  - cron: '0 8 * * *'   # Измените время
  - cron: '0 20 * * *'  # Измените время
```

### Добавление ключевых слов:

Отредактируйте `RELEVANT_KEYWORDS` в `menshealth_parser.py`.

### Изменение промпта для рерайтинга:

Отредактируйте `system_prompt` в функции `рерайтить_через_deepseek()`.

## 🐛 Отладка

### Локальный запуск с логами:

```bash
python3 menshealth_parser.py 2>&1 | tee parser.log
```

### Проверка состояния:

```bash
cat .menshealth_processed.json
```

### Тестирование парсинга одной статьи:

Можно временно изменить код, чтобы обработать конкретный URL.

## 📊 Логирование

Система выводит подробные логи:
- ✅ Успешные операции
- ❌ Ошибки
- ⚠️ Предупреждения
- ℹ️ Информация

## 🔒 Безопасность

- Все секреты хранятся в GitHub Secrets
- Не коммитьте `.menshealth_processed.json` в репозиторий (добавьте в `.gitignore`)
- API ключи не логируются

## 📈 Производительность

- Обрабатывает **1 статью за запуск** (чтобы не перегружать канал)
- Парсинг одной статьи: ~5-10 секунд
- Рерайтинг через DeepSeek: ~10-20 секунд
- Общее время: ~20-30 секунд на статью

## 🎯 Результат

Каждый запуск:
1. Парсит RSS фиды
2. Находит релевантные статьи
3. Обрабатывает первую новую статью
4. Публикует в Telegram канал

Если новых релевантных статей нет - просто пропускает.

---

**Автор:** VR-Lounge  
**Канал:** @fitnesstimer  
**Сайт:** https://www.tabatatimer.ru
