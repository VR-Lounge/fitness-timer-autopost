═══════════════════════════════════════════════════════════════
  ИНСТРУКЦИЯ: ЗАГРУЗКА НА YANDEX CLOUD И ЗАПУСК WORKFLOW
═══════════════════════════════════════════════════════════════

📋 ЧТО НУЖНО СДЕЛАТЬ:
───────────────────────────────────────────────────────────────
1. Загрузить обновлённые файлы на Yandex Cloud через aws cli
2. Запустить workflow вручную через GitHub Actions

───────────────────────────────────────────────────────────────
ШАГ 1: ПОЛУЧИТЬ КЛЮЧИ ДОСТУПА К YANDEX CLOUD
───────────────────────────────────────────────────────────────

1. Откройте GitHub Secrets:
   https://github.com/VR-Lounge/fitness-timer-autopost/settings/secrets/actions

2. Найдите секреты:
   - YANDEX_ACCESS_KEY_ID
   - YANDEX_SECRET_ACCESS_KEY

3. Скопируйте их значения (нажмите "Reveal" для просмотра)

───────────────────────────────────────────────────────────────
ШАГ 2: УСТАНОВИТЬ ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ
───────────────────────────────────────────────────────────────

В терминале выполните:

export YANDEX_ACCESS_KEY_ID="ваш_access_key_id"
export YANDEX_SECRET_ACCESS_KEY="ваш_secret_access_key"

⚠️ ВАЖНО: Замените значения на реальные из GitHub Secrets!

───────────────────────────────────────────────────────────────
ШАГ 3: ЗАГРУЗИТЬ ФАЙЛЫ НА YANDEX CLOUD
───────────────────────────────────────────────────────────────

1. Перейдите в директорию проекта:
   cd "/Users/LOBANOFF-PRO/Documents/TABATATIMER.RU/С MediaPipe/fitness-timer-autopost"

2. Сделайте скрипт исполняемым:
   chmod +x upload_to_yandex_manual.sh

3. Запустите скрипт:
   bash upload_to_yandex_manual.sh

Или выполните команды вручную:

cd "../public_html"

# Загрузить blog-posts.json
aws s3 cp blog-posts.json "s3://www.tabatatimer.ru/blog-posts.json" \
    --endpoint-url="https://storage.yandexcloud.net" \
    --acl public-read

# Загрузить blog.html
aws s3 cp blog.html "s3://www.tabatatimer.ru/blog.html" \
    --endpoint-url="https://storage.yandexcloud.net" \
    --acl public-read

# Загрузить HTML страницы статей
aws s3 sync blog/ "s3://www.tabatatimer.ru/blog/" \
    --endpoint-url="https://storage.yandexcloud.net" \
    --acl public-read \
    --exclude "*.DS_Store"

# Загрузить изображения блога
aws s3 sync images/blog/ "s3://www.tabatatimer.ru/images/blog/" \
    --endpoint-url="https://storage.yandexcloud.net" \
    --acl public-read \
    --exclude "*.DS_Store"

───────────────────────────────────────────────────────────────
ШАГ 4: ЗАПУСТИТЬ WORKFLOW ВРУЧНУЮ
───────────────────────────────────────────────────────────────

⚠️ ВАЖНО: Workflow НЕ запускается через aws cli!
Workflow запускается через GitHub Actions UI или API.

ВАРИАНТ 1: ЧЕРЕЗ GITHUB UI (РЕКОМЕНДУЕТСЯ)
───────────────────────────────────────────────────────────────

1. Откройте репозиторий:
   https://github.com/VR-Lounge/fitness-timer-autopost

2. Перейдите в раздел "Actions"

3. Выберите нужный workflow:
   - 💅 Парсер Women's Health (womenshealth-parser.yml)
   - 💪 Парсер Men's Health (menshealth-parser.yml)

4. Нажмите "Run workflow" (справа)

5. Выберите ветку (обычно "main" или "master")

6. Нажмите "Run workflow"

✅ Workflow запустится автоматически!

ВАРИАНТ 2: ЧЕРЕЗ GITHUB API (КОМАНДНАЯ СТРОКА)
───────────────────────────────────────────────────────────────

1. Получите GitHub Personal Access Token (PAT):
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Создайте новый токен с правами: repo, workflow

2. Установите переменную окружения:
   export GITHUB_TOKEN="ваш_personal_access_token"

3. Запустите workflow через API:

# Для Women's Health Parser:
curl -X POST \
  -H "Accept: application/vnd.github+json" \
  -H "Authorization: Bearer $GITHUB_TOKEN" \
  https://api.github.com/repos/VR-Lounge/fitness-timer-autopost/actions/workflows/womenshealth-parser.yml/dispatches \
  -d '{"ref":"main"}'

# Для Men's Health Parser:
curl -X POST \
  -H "Accept: application/vnd.github+json" \
  -H "Authorization: Bearer $GITHUB_TOKEN" \
  https://api.github.com/repos/VR-Lounge/fitness-timer-autopost/actions/workflows/menshealth-parser.yml/dispatches \
  -d '{"ref":"main"}'

───────────────────────────────────────────────────────────────
✅ ПРОВЕРКА РЕЗУЛЬТАТОВ
───────────────────────────────────────────────────────────────

1. Проверьте сайт:
   https://www.tabatatimer.ru/blog.html

2. Проверьте GitHub Actions:
   https://github.com/VR-Lounge/fitness-timer-autopost/actions

3. Проверьте логи workflow:
   - Откройте запущенный workflow
   - Проверьте каждый шаг на наличие ошибок

───────────────────────────────────────────────────────────────
🔧 УСТРАНЕНИЕ ПРОБЛЕМ
───────────────────────────────────────────────────────────────

ПРОБЛЕМА: "AWS CLI не установлен"
РЕШЕНИЕ:
  macOS: brew install awscli
  Linux: sudo apt-get install awscli

ПРОБЛЕМА: "Access Denied" при загрузке
РЕШЕНИЕ:
  - Проверьте правильность YANDEX_ACCESS_KEY_ID и YANDEX_SECRET_ACCESS_KEY
  - Убедитесь, что ключи активны в Yandex Cloud Console

ПРОБЛЕМА: "Workflow не запускается"
РЕШЕНИЕ:
  - Используйте GitHub UI (вариант 1) - самый надёжный способ
  - Проверьте, что workflow имеет `workflow_dispatch` в секции `on:`

───────────────────────────────────────────────────────────────
📝 БЫСТРАЯ КОМАНДА ДЛЯ ЗАГРУЗКИ
───────────────────────────────────────────────────────────────

Если у вас уже настроены переменные окружения:

cd "/Users/LOBANOFF-PRO/Documents/TABATATIMER.RU/С MediaPipe/fitness-timer-autopost"
bash upload_to_yandex_manual.sh

═══════════════════════════════════════════════════════════════
Готово! После загрузки файлов и запуска workflow изменения
появятся на сайте https://www.tabatatimer.ru
═══════════════════════════════════════════════════════════════
