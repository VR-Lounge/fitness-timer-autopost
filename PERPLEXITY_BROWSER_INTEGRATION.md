# 🌐 ИНТЕГРАЦИЯ CURSOR + PERPLEXITY/COMET BROWSER

## 🎯 Цель

Создать систему автоматической синхронизации между Cursor и Perplexity/Comet Browser для максимально автономной работы.

> **🚀 БЫСТРЫЙ СТАРТ:**  
> - Для Perplexity: Читай `PERPLEXITY_INSTRUCTIONS.md`  
> - Промпт для передачи: `PERPLEXITY_PROMPT.md`  
> - Текущие задачи: `PERPLEXITY_TASKS.md`

## 🔧 Настройка

### 1. Cursor Side (Автоматическая генерация инструкций)

Cursor автоматически генерирует инструкции для Perplexity через файлы:
- `browser_integration.py` - создает инструкции
- `autonomous_monitor.py` - мониторит и создает задачи

### 2. Perplexity/Comet Browser Side

Perplexity должен:
1. Читать инструкции из файлов `browser_test_*.md`
2. Выполнять проверки
3. Сохранять результаты в `browser_results_*.json`

## 📋 Формат инструкций

### Структура файла инструкций:

```markdown
# 🌐 ИНСТРУКЦИИ ДЛЯ PERPLEXITY/COMET BROWSER

## Задача: [описание]

### ШАГ 1: [действие]
1. [подшаг]
2. [подшаг]

### ШАГ 2: [действие]
...

### ОТЧЁТ
[формат отчета]
```

## 🔄 Автоматический цикл работы

### Cursor → Perplexity:

1. **Cursor генерирует инструкции:**
   ```bash
   python3 browser_integration.py
   ```
   Создает файлы: `browser_test_*.md`

2. **Perplexity читает инструкции:**
   - Открывает файлы `browser_test_*.md`
   - Выполняет проверки
   - Сохраняет результаты в `browser_results_*.json`

3. **Cursor читает результаты:**
   - Анализирует `browser_results_*.json`
   - Применяет исправления
   - Генерирует новые инструкции при необходимости

## 📊 Формат результатов

### browser_results_*.json:

```json
{
  "url": "https://www.tabatatimer.ru",
  "timestamp": "2026-01-09T12:00:00",
  "console_errors": [
    {
      "message": "Error: ...",
      "line": 123,
      "file": "script.js"
    }
  ],
  "network_errors": [
    {
      "url": "https://...",
      "status": 404,
      "type": "failed"
    }
  ],
  "screenshot": "screenshot_tabatatimer_ru.png",
  "functionality_tests": {
    "timer_works": true,
    "forms_work": true,
    "mobile_responsive": true
  },
  "status": "passed" | "failed" | "partial"
}
```

## 🤖 Автоматизация через GitHub Actions

### Workflow для синхронизации:

```yaml
name: 🔄 Cursor-Perplexity Sync

on:
  schedule:
    - cron: '0 */2 * * *'  # Каждые 2 часа
  workflow_dispatch:

jobs:
  generate-instructions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Генерация инструкций
        run: |
          cd fitness-timer-autopost
          python3 browser_integration.py
      - name: Commit инструкции
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add browser_test_*.md
          git commit -m "Auto: Обновлены инструкции для Perplexity" || exit 0
          git push
```

## 📝 Чеклист для Perplexity

### При получении инструкций:

1. ✅ Открыть файл `browser_test_*.md`
2. ✅ Прочитать инструкции
3. ✅ Выполнить все шаги
4. ✅ Создать отчет в формате JSON
5. ✅ Сохранить как `browser_results_*.json`
6. ✅ Закоммитить результаты в Git

### При проверке сайта:

1. ✅ Открыть сайт в Comet Browser
2. ✅ Открыть DevTools (F12)
3. ✅ Проверить Console на ошибки
4. ✅ Проверить Network на failed запросы
5. ✅ Протестировать функционал
6. ✅ Сделать скриншот
7. ✅ Заполнить JSON отчет

## 🔗 Интеграция с существующими системами

### С автономным тестированием:

```python
# autonomous_testing.py может вызывать browser_integration.py
from browser_integration import BrowserTester

tester = BrowserTester()
instructions = tester.generate_browser_instructions(url)
# Сохраняет инструкции для Perplexity
```

### С мониторингом:

```python
# autonomous_monitor.py читает результаты от Perplexity
import json
from pathlib import Path

results_file = Path('browser_results_*.json')
if results_file.exists():
    with open(results_file) as f:
        results = json.load(f)
    # Анализирует результаты и применяет исправления
```

## 🎯 Примеры использования

### Пример 1: Проверка сайта после деплоя

```bash
# Cursor генерирует инструкции
python3 browser_integration.py

# Perplexity выполняет проверку (вручную или автоматически)
# Результаты сохраняются в browser_results_*.json

# Cursor анализирует результаты
python3 autonomous_monitor.py
```

### Пример 2: Автоматическая проверка каждые 2 часа

GitHub Actions workflow:
1. Генерирует инструкции
2. Коммитит их
3. Perplexity (через другой workflow или вручную) выполняет
4. Результаты анализируются

## 🚀 Будущие улучшения

1. **Прямая интеграция через API:**
   - Cursor → API → Perplexity
   - Автоматический обмен данными

2. **Webhook для уведомлений:**
   - Perplexity отправляет webhook когда готовы результаты
   - Cursor автоматически обрабатывает

3. **Общая база данных:**
   - Хранить инструкции и результаты в БД
   - Синхронизация через API

## 📚 Документация

- **Cursor инструкции:** `browser_integration.py`
- **Perplexity инструкции:** `browser_test_*.md`
- **Результаты:** `browser_results_*.json`
- **Мониторинг:** `autonomous_monitor.py`

---

**Автор:** VR-Lounge  
**Версия:** 1.0  
**Дата:** 2026-01-09
