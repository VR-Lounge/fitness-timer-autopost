# 🚀 Миграция сайта tabatatimer.ru на Yandex Cloud Object Storage

## 📋 Статус

- ✅ Бакет создан: `www.tabatatimer.ru`
- ✅ Хостинг статического сайта настроен с индексным документом `index.html`
- ✅ DNS обновлен: `www.tabatatimer.ru` → `www.tabatatimer.ru.website.yandexcloud.net`
- ⏳ **Осталось: загрузить файлы сайта**

## 📂 Локальные файлы

**Расположение:** `/Users/LOBANOFF-PRO/Documents/TABATATIMER.RU/С MediaPipe/public_html`

**Статистика:**
- Файлов: **1647**
- Размер: **575 MB**
- Главный файл: `index.html` ✅

## 🔧 Шаг 1: Настройка AWS CLI для Yandex Cloud

### 1.1. Получение ключей доступа

1. Перейдите в консоль Yandex Cloud: https://console.yandex.cloud/
2. Выберите каталог (например: `cloud-admintabatatimerru` или `default`)
3. Перейдите в раздел **"Сервисные аккаунты"** (Service Accounts)
4. Создайте новый сервисный аккаунт или используйте существующий
5. Создайте **статический ключ доступа** (Access Key)
6. Сохраните:
   - **Access Key ID** (например: `YCAJEp...`)
   - **Secret Access Key** (например: `YCOJ...`)

### 1.2. Настройка профиля

**Вариант А: Автоматический скрипт (рекомендуется)**

```bash
cd "/Users/LOBANOFF-PRO/Documents/TABATATIMER.RU/С MediaPipe/fitness-timer-autopost"
./setup_yandex_cloud.sh
```

Скрипт попросит ввести:
- Access Key ID
- Secret Access Key

**Вариант Б: Ручная настройка**

```bash
aws configure --profile yandex
```

Введите:
- **AWS Access Key ID:** [ваш Access Key ID из Yandex Cloud]
- **AWS Secret Access Key:** [ваш Secret Access Key из Yandex Cloud]
- **Default region name:** `ru-central1`
- **Default output format:** `json`

### 1.3. Проверка настройки

```bash
aws s3 ls --endpoint-url=https://storage.yandexcloud.net --profile yandex
```

Должен показать список бакетов, включая `www.tabatatimer.ru`.

---

## 📤 Шаг 2: Загрузка файлов

### 2.1. Автоматическая загрузка (рекомендуется)

```bash
cd "/Users/LOBANOFF-PRO/Documents/TABATATIMER.RU/С MediaPipe/fitness-timer-autopost"
./upload_to_yandex_cloud.sh
```

Скрипт:
- ✅ Проверит настройку профиля
- ✅ Подсчитает файлы для загрузки
- ✅ Загрузит все файлы с публичным доступом
- ✅ Покажет прогресс и результат

### 2.2. Ручная загрузка

```bash
cd "/Users/LOBANOFF-PRO/Documents/TABATATIMER.RU/С MediaPipe/public_html"

aws s3 sync . s3://www.tabatatimer.ru \
  --endpoint-url=https://storage.yandexcloud.net \
  --profile yandex \
  --acl public-read \
  --exclude ".git/*" \
  --exclude ".DS_Store" \
  --exclude "*.log" \
  --exclude ".gitignore" \
  --exclude "node_modules/*" \
  --delete
```

**Параметры команды:**
- `.` - текущая папка (все файлы)
- `s3://www.tabatatimer.ru` - имя бакета в Yandex Cloud
- `--endpoint-url=https://storage.yandexcloud.net` - адрес API Yandex Cloud
- `--profile yandex` - использовать профиль с ключами Yandex Cloud
- `--acl public-read` - сделать файлы публично доступными
- `--delete` - удалить файлы из бакета, которых нет локально

---

## ✅ Шаг 3: Проверка результата

### 3.1. Проверка в консоли Yandex Cloud

1. Перейдите: https://console.yandex.cloud/folders/b1gmsesnb2h7cs5c7qov/storage/buckets/www.tabatatimer.ru
2. Должны быть видны все файлы и папки:
   - `index.html` (в корне)
   - `favicon.ico`
   - Папки: `assets/`, `images/`, `ai-training-module/`, и т.д.

### 3.2. Проверка прямого URL Yandex Cloud

```bash
curl -I http://www.tabatatimer.ru.website.yandexcloud.net
```

Должен вернуть `HTTP/1.1 200 OK` и открыть сайт.

### 3.3. Проверка основного домена

После распространения DNS (1-2 часа):

```bash
curl -I https://www.tabatatimer.ru
```

Должен вернуть `HTTP/1.1 200 OK` и открыть сайт **с корневого URL** (без `/index.html`).

---

## 📊 Что будет загружено

**Все файлы сайта, включая:**
- ✅ `index.html` (главный файл)
- ✅ `favicon.ico`
- ✅ Папки: `assets/`, `images/`, `ai-training-module/`, `PHPMailer/`, `cgi-bin/`
- ✅ Все CSS, JS, шрифты
- ✅ Все музыкальные файлы (46 MP3)
- ✅ Все изображения (упражнения, тренировки)
- ✅ Файлы конфигурации (`_config.yml`, `CNAME`, `robots.txt`)
- ✅ Документация (README.md, MIGRATION_GUIDE.md и т.д.)

**Исключения (не загружаются):**
- `.git/` - файлы Git
- `.DS_Store` - системные файлы macOS
- `*.log` - лог-файлы
- `.gitignore` - файл Git
- `node_modules/` - зависимости Node.js (если есть)

---

## 🚨 Важные замечания

1. **ACL (публичный доступ):** Флаг `--acl public-read` критически важен - без него файлы будут недоступны публично
2. **Индексный файл:** Файл `index.html` ОБЯЗАТЕЛЬНО должен быть в корне бакета
3. **Структура папок:** Сохраняется точно такая же структура, как локально
4. **Кодировка:** Файлы должны быть в UTF-8 (особенно для русского текста)
5. **Время загрузки:** Для 1647 файлов (575 MB) загрузка займет **5-15 минут** в зависимости от скорости интернета

---

## 🔍 Полезные команды

### Проверка списка файлов в бакете

```bash
aws s3 ls s3://www.tabatatimer.ru \
  --endpoint-url=https://storage.yandexcloud.net \
  --profile yandex \
  --recursive | head -20
```

### Проверка конкретного файла

```bash
aws s3 ls s3://www.tabatatimer.ru/index.html \
  --endpoint-url=https://storage.yandexcloud.net \
  --profile yandex
```

### Загрузка одного файла

```bash
aws s3 cp index.html s3://www.tabatatimer.ru/index.html \
  --endpoint-url=https://storage.yandexcloud.net \
  --profile yandex \
  --acl public-read
```

### Удаление файла из бакета

```bash
aws s3 rm s3://www.tabatatimer.ru/filename.txt \
  --endpoint-url=https://storage.yandexcloud.net \
  --profile yandex
```

---

## 📝 После загрузки

1. ✅ Проверьте сайт по прямому URL Yandex Cloud
2. ⏳ Подождите 1-2 часа для распространения DNS
3. ✅ Проверьте основной домен: `https://www.tabatatimer.ru`
4. ✅ Проверьте корневой URL (должен работать без `/index.html`)
5. ✅ Вернитесь в Яндекс.Вебмастер → Права доступа → Подтвердить
6. ✅ После подтверждения → Индексирование → Переезд сайта → Установить `www.tabatatimer.ru` как главное зеркало

---

## 🔗 Полезные ссылки

- Документация Yandex Cloud Object Storage: https://yandex.cloud/ru/docs/storage/
- AWS CLI для Yandex Cloud: https://yandex.cloud/ru/docs/storage/tools/aws-cli
- Настройка статического хостинга: https://yandex.cloud/ru/docs/storage/operations/hosting/setup
- Консоль Yandex Cloud: https://console.yandex.cloud/

---

## 🆘 Решение проблем

### Ошибка "InvalidAccessKeyId"
- Проверьте правильность Access Key ID и Secret Access Key
- Убедитесь, что используете правильный профиль: `--profile yandex`

### Ошибка "NoSuchBucket"
- Убедитесь, что бакет `www.tabatatimer.ru` существует в Yandex Cloud
- Проверьте правильность имени бакета

### Ошибка "AccessDenied"
- Проверьте права доступа сервисного аккаунта
- Убедитесь, что сервисный аккаунт имеет роль `storage.editor` или `storage.admin`

### Файлы загружены, но сайт не открывается
- Проверьте настройку статического хостинга в консоли Yandex Cloud
- Убедитесь, что индексный документ установлен как `index.html`
- Проверьте DNS-настройки

---

**Готово к загрузке!** 🚀

