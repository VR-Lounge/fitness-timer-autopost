# 🔧 Решение проблемы DNS_PROBE_FINISHED_NXDOMAIN для tabatatimer.ru

## ✅ Статус серверной инфраструктуры

Все серверные настройки **корректны**:
- ✅ GitHub Pages активен и работает
- ✅ DNS в Selectel настроен правильно (4 A-записи на IP GitHub Pages)
- ✅ Домен делегирован в nic.ru
- ✅ Глобально DNS резолвится отлично по всему миру

**Проблема исключительно локальная** — DNS-кэш на Mac или DNS-over-HTTPS в браузере.

---

## 🚀 Быстрое решение (выбери один вариант)

### Вариант 1: Скрипт через терминал (рекомендуется)

```bash
cd "/Users/LOBANOFF-PRO/Documents/TABATATIMER.RU/С MediaPipe/fitness-timer-autopost"
sudo ./CLEAR_DNS_CACHE.sh
```

**Или выполни команды вручную:**
```bash
sudo dscacheutil -flushcache
sudo killall -HUP mDNSResponder
```

После выполнения:
1. Перезапусти браузер
2. Открой: `https://tabatatimer.ru`

---

### Вариант 2: Очистка DNS-кэша в браузере (без sudo)

#### Chrome / Edge / Brave:
1. Открой новую вкладку
2. В адресной строке введи: `chrome://net-internals/#dns`
3. Нажми кнопку **"Clear host cache"**
4. Перезапусти браузер

#### Firefox:
1. Открой новую вкладку
2. В адресной строке введи: `about:networking#dns`
3. Нажми кнопку **"Clear DNS Cache"**
4. Перезапусти браузер

#### Safari:
1. Меню → Разработка → Очистить кэши
2. Если меню "Разработка" не видно: Настройки → Дополнения → Включить меню "Разработка"

---

### Вариант 3: Отключение DNS-over-HTTPS (DoH)

Если DoH включён, браузер может использовать устаревший кэш Cloudflare/Google.

#### Chrome / Edge:
1. Открой: `chrome://settings/security`
2. Найди раздел: **"Использовать безопасный DNS"**
3. Выключи или выбери: **"С вашим текущим поставщиком услуг"**
4. Перезапусти браузер

#### Opera:
1. Opera → Preferences → System
2. Отключи: **"Use DNS-over-HTTPS instead of the system's DNS settings"**
3. Перезапусти браузер

#### Firefox:
1. Открой: `about:preferences#general`
2. Прокрути вниз → Параметры сети → Настройки
3. Сними галочку: **"Включить DNS через HTTPS"**
4. Перезапусти браузер

---

### Вариант 4: Проверка через приватное окно

Приватное окно не использует DNS-кэш браузера:
- **Chrome/Safari:** `Cmd + Shift + N`
- **Firefox:** `Cmd + Shift + P`

Открой в приватном окне: `https://tabatatimer.ru`

---

### Вариант 5: Изменение DNS-серверов в системе

Если ничего не помогло, попробуй использовать публичные DNS:

1. Системные настройки → Сеть
2. Выбери активное подключение (Wi-Fi/Ethernet)
3. Нажми "Дополнительно" → вкладка "DNS"
4. Добавь DNS-серверы:
   - `8.8.8.8` (Google)
   - `1.1.1.1` (Cloudflare)
5. Перезапусти Wi-Fi/подключение

---

## 🔍 Проверка после исправления

Выполни в терминале:
```bash
nslookup tabatatimer.ru
```

**Ожидаемый результат:**
```
Name:    tabatatimer.ru
Address: 185.199.108.153
Address: 185.199.109.153
Address: 185.199.110.153
Address: 185.199.111.153
```

Если видишь эти IP-адреса — DNS работает корректно!

---

## 📋 Порядок действий (рекомендуемый)

1. ✅ **Попробуй Вариант 2** (очистка кэша браузера) — быстро, без sudo
2. ✅ **Если не помогло** → Вариант 1 (скрипт через терминал)
3. ✅ **Проверь DoH** → Вариант 3 (отключение DNS-over-HTTPS)
4. ✅ **Перезапусти браузер** после каждого шага
5. ✅ **Попробуй приватное окно** для моментальной проверки

---

## ✅ Ожидаемый результат

После очистки DNS-кэша сайт `tabatatimer.ru` должен открыться **немедленно**, так как:
- GitHub Pages активен и работает
- DNS настроен корректно
- Глобально резолвится отлично

**Проблема была исключительно в локальном кэше.**

---

## 📊 Резюме диагностики

| Компонент | Статус |
|-----------|--------|
| GitHub Pages | ✅ Работает |
| DNS Selectel | ✅ Настроен (4 A-записи) |
| Домен nic.ru | ✅ Делегирован |
| DNS Global | ✅ Резолвится по всему миру |
| Локальный доступ | ❌ Кэш DNS/DoH (исправляется) |

---

## 🆘 Если ничего не помогло

1. Перезагрузи Mac
2. Проверь файрвол и антивирус (могут блокировать DNS)
3. Попробуй другой браузер
4. Проверь, не используешь ли VPN (может влиять на DNS)

---

**Создано:** 23.12.2025  
**Источник:** Диагностика от Comet (Perplexity) + Cursor

