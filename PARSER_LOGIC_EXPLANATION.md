# 📋 ЛОГИКА РАБОТЫ ПАРСЕРА: ПРАВИЛА ПОИСКА И ВЫБОРА СТАТЕЙ

## 1️⃣ ПРАВИЛА ПОИСКА РЕЛЕВАНТНЫХ СТАТЕЙ ИЗ RSS ФИДОВ

### ЭТАП 1: Парсинг RSS фидов
- Парсер проходит по **101 RSS фиду** (48 для девушек + 53 для мужчин)
- Поддерживает форматы: RSS 2.0, Atom, FeedBurner
- Извлекает из каждого фида: `title`, `link`, `description`, `pubDate`
- Удаляет дубликаты по URL

### ЭТАП 2: Фильтрация по релевантности

**Правило:** Статья считается релевантной, если в `title` или `description` найдено **хотя бы одно** ключевое слово из списка:

#### Для девушек (`RELEVANT_KEYWORDS` в `womenshealth_parser.py`):
```python
# Интервальные тренировки
'tabata', 'hiit', 'amrap', 'emom', 'interval training', 'interval workout',
'high intensity', 'circuit training', 'timed workout', 'workout timer',

# Диеты и питание
'diet', 'nutrition', 'meal plan', 'protein', 'carb', 'calorie',
'weight loss', 'fat loss', 'metabolism', 'meal prep', 'healthy eating',

# Упражнения и тренировки
'workout', 'exercise', 'training', 'fitness', 'cardio', 'strength',
'endurance', 'conditioning', 'burn fat', 'build muscle', 'toning',

# Женское здоровье
'women health', 'female fitness', 'hormones', 'period', 'menstrual',
'pregnancy workout', 'postpartum', 'menopause', 'women wellness',

# Связанные темы
'quick workout', 'home workout', 'bodyweight', 'no equipment',
'short workout', 'efficient workout', 'effective training', 'yoga', 'pilates'
```

#### Для мужчин (`RELEVANT_KEYWORDS` в `menshealth_parser.py`):
```python
# Интервальные тренировки
'tabata', 'hiit', 'amrap', 'emom', 'interval training', 'interval workout',
'high intensity', 'circuit training', 'timed workout', 'workout timer',

# Диеты и питание
'diet', 'nutrition', 'meal plan', 'protein', 'carb', 'calorie',
'weight loss', 'fat loss', 'metabolism', 'meal prep',

# Упражнения и тренировки
'workout', 'exercise', 'training', 'fitness', 'cardio', 'strength',
'endurance', 'conditioning', 'burn fat', 'build muscle',

# Связанные темы
'quick workout', 'home workout', 'bodyweight', 'no equipment',
'short workout', 'efficient workout', 'effective training'
```

**Функция:** `проверить_релевантность(статья)` в `womenshealth_parser.py:438`
- Проверяет `title + description` на наличие ключевых слов
- Возвращает `(релевантна: bool, найденные_ключевые_слова: list)`

### ЭТАП 3: Фильтрация уже обработанных статей
- Проверяет URL статьи в `.womenshealth_processed.json` / `.menshealth_processed.json`
- Пропускает статьи, которые уже были обработаны ранее

---

## 2️⃣ КАК ОПРЕДЕЛЯЕТСЯ ТЕМАТИКА АВТОМАТИЧЕСКИ ПО КОНТЕНТУ

**Функция:** `определить_теги(текст, заголовок, источник)` в `womenshealth_parser.py:882`

### ЛОГИКА ОПРЕДЕЛЕНИЯ ТЕГОВ:

#### 1. АУДИТОРИЯ (обязательный тег):
- **"Девушкам"** - если в тексте есть:
  - Слова: `девушк*`, `женщин*`, `для девочек`, `женский`
  - Контекст: `тренировки для девушек`, `женское здоровье`, `похудение для девушек`
  - Или источник = `womenshealth` (по умолчанию)

- **"Мужчинам"** - если в тексте есть:
  - Слова: `мужчин*`, `мужской`, `для парней`, `мужское`
  - Контекст: `набор массы`, `силовые тренировки`, `мужское здоровье`, `тестостерон`
  - Или источник = `menshealth` (по умолчанию)

#### 2. ТЕМАТИКИ (определяются по ключевым словам):

**💪 "Тренировка"** - если есть:
```python
'тренировк*', 'упражнен*', 'программа тренировок', 'программа',
'подход', 'повторен*', 'раунд', 'серия', 'цикл',
'бурпи', 'приседан*', 'отжиман*', 'планка', 'выпад',
'скручиван*', 'вакуум', 'присед', 'жим',
'тяга', 'бицепс', 'трицепс', 'пресс', 'ягодиц',
'табата', 'hiit', 'emom', 'amrap', 'интервал',
'силовой', 'кардио', 'гимнастик*', 'фитнес',
'разминка', 'заминка', 'растяжка'
```

**🥗 "Питание"** - если есть:
```python
'рецепт*', 'питани*', 'еда', 'блюд*', 'продукт*', 'ингредиент',
'жкт', 'кишечник', 'желудок', 'пищеварен*', 'желудочно-кишечн*',
'белок', 'углевод', 'жир', 'клетчатка', 'воды', 'водой',
'завтрак', 'обед', 'ужин', 'перекус', 'меню', 'рацион',
'витамин*', 'минерал*', 'протеин', 'макро', 'микро',
'овощ*', 'фрукт*', 'мясо', 'рыба', 'яйцо', 'молочн*',
'до тренировки', 'после тренировки'
```

**🥑 "Диеты"** - если есть:
```python
'диет*', 'похуден*', 'калори*', 'дефицит калори*', 'профицит',
'кето', 'палео', 'вегетариан*', 'веган*', 'средиземноморск*',
'потеря веса', 'сброс веса', 'снижение веса',
'бжу', 'баланс', 'макрос*', 'микрос*'
```

**💫 "Мотивация"** - если есть:
```python
'мотивац*', 'вдохнов*', 'мотивир*',
'результат', 'цель', 'успех', 'философия',
'система', 'принцип', 'лайфхак', 'совет',
'начни', 'начинай', 'не откладывай', 'сегодня',
'психологи*', 'ментальн*', 'настрой', 'мышление'
```

**Fallback:** Если тегов нет, добавляется `"Мотивация"` по умолчанию.

---

## 3️⃣ КАК ПРОИСХОДИТ ВЫБОР ПУБЛИКАЦИИ ПЕРВОЙ УНИКАЛЬНОЙ СТАТЬИ

**Функция:** `главная()` в `womenshealth_parser.py:1275`

### ПОСЛЕДОВАТЕЛЬНОСТЬ ДЕЙСТВИЙ:

#### ШАГ 1: Получение релевантных статей
```python
релевантные = []  # Список статей, прошедших фильтрацию
```

#### ШАГ 2: Обработка до 5 статей (максимум)
```python
максимальное_количество_попыток = min(5, len(релевантные))
```

**⚠️ КРИТИЧНО:** Парсер обрабатывает **только первые 5 релевантных статей** в порядке их появления из RSS фидов.

#### ШАГ 3: Для каждой статьи (по порядку):

1. **Парсинг полного текста и изображений**
   - `парсить_статью(статья['link'])`
   - Если нет контента → пропуск, следующая статья
   - Если нет изображений → пропуск, следующая статья

2. **Рерайтинг через DeepSeek AI**
   - `рерайтить_через_deepseek(parsed['content'], статья['title'])`
   - Если рерайтинг не удался → пропуск, следующая статья

3. **Расширение контента для сайта**
   - `расширить_контент_для_статьи(parsed['content'], статья['title'])`
   - Если расширение не удалось → используется короткий текст

4. **Проверка уникальности** (если `PUBLISH_TO_BLOG=true`)
   - `сохранить_пост_в_блог(...)` → вызывает `проверить_полную_уникальность(...)`
   - Проверки:
     - ✅ Хеш пары ФОТО+ТЕКСТ
     - ✅ Хеш текста отдельно
     - ✅ Хеш изображения отдельно
     - ✅ Семантическая схожесть через DeepSeek (если > 80% → дубликат)
     - ✅ Проверка по темам (например, "гибридный_атлет")
     - ✅ Проверка по персонажам (например, "brandon")
   - Если не уникален → пропуск, следующая статья

5. **Выбор изображения через DeepSeek AI**
   - `выбрать_лучшее_изображение_для_контента(...)`
   - Проверяет уникальность изображения
   - Оценивает релевантность изображения контенту через DeepSeek
   - Если нет уникального и релевантного изображения → пропуск, следующая статья

6. **Публикация**
   - Если все проверки пройдены → публикация в Telegram и (опционально) на сайт
   - Статья помечается как обработанная
   - **Цикл прерывается** (публикуется только одна статья за запуск)

---

## 4️⃣ ПОЧЕМУ DEEPSEEK НЕ АНАЛИЗИРУЕТ ВЕСЬ ПАРСИНГ НА ПРЕДМЕТ БАЛАНСА РАЗНООБРАЗИЯ ТЕМАТИК?

### ТЕКУЩАЯ ЛОГИКА:
❌ **НЕТ анализа баланса тематик**
- Парсер берет **первую** уникальную статью из первых 5 релевантных
- Не учитывает, какие тематики уже были опубликованы
- Не стремится к разнообразию (может быть 5 статей подряд про "Тренировка")

### ПРИЧИНЫ:
1. **Производительность:** Анализ всех статей через DeepSeek для баланса тематик потребовал бы:
   - Множественные API-вызовы к DeepSeek (дорого по токенам)
   - Анализ последних N опубликованных статей
   - Сравнение тематик всех релевантных статей
   - Выбор статьи с наименее представленной тематикой

2. **Текущая архитектура:**
   - Парсер обрабатывает статьи **последовательно** (одну за другой)
   - Останавливается на **первой** успешной публикации
   - Не имеет "глобального видения" всех доступных статей

3. **Ограничение попыток:**
   - `максимальное_количество_попыток = min(5, len(релевантные))`
   - Даже если есть 100 релевантных статей, проверяются только первые 5

### ЧТО МОЖНО УЛУЧШИТЬ:

#### Вариант 1: Предварительный анализ тематик (без DeepSeek)
```python
# 1. Определить тематики всех релевантных статей (быстро, без DeepSeek)
темы_всех_статей = []
for статья in релевантные[:20]:  # Анализируем больше статей
    теги = определить_теги(статья['title'], статья['description'])
    темы_всех_статей.append({
        'статья': статья,
        'теги': теги
    })

# 2. Подсчитать, какие тематики уже были опубликованы (из blog-posts.json)
опубликованные_темы = подсчитать_темы_последних_публикаций(последние_10_постов)

# 3. Выбрать статью с наименее представленной тематикой
лучшая_статья = выбрать_статью_для_баланса(темы_всех_статей, опубликованные_темы)
```

#### Вариант 2: Использование DeepSeek для анализа баланса (дороже)
```python
# 1. Получить последние 10 опубликованных статей
последние_статьи = получить_последние_публикации(10)

# 2. Для каждой релевантной статьи (до 10) определить тематику через DeepSeek
# 3. Выбрать статью, тематика которой наименее представлена в последних публикациях
```

#### Вариант 3: Приоритизация по тематикам (простой)
```python
# Определить приоритет тематик на основе расписания
приоритеты_тем = {
    '08:00 UTC': ['Тренировка', 'Мотивация'],  # Утро - тренировки
    '12:00 UTC': ['Питание', 'Диеты'],         # День - питание
    '18:00 UTC': ['Мотивация', 'Тренировка'],  # Вечер - мотивация
    '20:00 UTC': ['Диеты', 'Питание']         # Поздний вечер - диеты
}

# Выбрать статью с тематикой, соответствующей текущему времени
```

---

## 📊 ТЕКУЩАЯ СТАТИСТИКА:

- **Всего RSS фидов:** 101 (48 для девушек + 53 для мужчин)
- **Максимум попыток:** 5 статей за запуск
- **Публикаций в день:** 6 (3 на сайт + 3 в Telegram)
- **Проверка уникальности:** ✅ Есть (хеши + DeepSeek семантика)
- **Проверка баланса тематик:** ❌ Нет

---

## 🎯 РЕКОМЕНДАЦИИ:

1. **Увеличить количество анализируемых статей** с 5 до 10-20
2. **Добавить простой анализ баланса тематик** (без DeepSeek, на основе тегов)
3. **Приоритизировать статьи** по тематикам в зависимости от времени публикации
4. **Логировать статистику тематик** для мониторинга баланса
